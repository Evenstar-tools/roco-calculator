# 契约的形状特性实施计划

## 任务 1：纯规则与单元测试

- 新增 `src/domain/contract-shape.js`。
- 先写失败测试，覆盖十三种球、棱镜半值、无效选择和非连击技能。
- 实现纯解析器，不读取 UI 或全局状态。

## 任务 2：特性控件与配置记忆

- 在 `trait-effects.js` 为“契约的形状”提供咕噜球及棱镜效果选择。
- 扩展 `TraitInputs` 的条件显示，四技能与单技能一致。
- 用特性字段测试确认方向切换和配置库语义键兼容。

## 任务 3：伤害计算接入

- 在 `calculate.js` 接入双方效果：能力等级、速度、固定威力、连击、中毒和星陨。
- 特性结算说明进入结果栏，公式来源增加独立规则标识。
- 先补失败的集成测试，再实现最小计算改动。

## 任务 4：回归验收

- 运行定向领域、状态和 UI 测试。
- 运行 `npm test`、`npm run build`、`git diff --check`。
- 检查差异只包含本特性及此前工作区已有改动，不打包、不提交。
