# 技能使用摘要修复 · 2026-09-13

## 本次边界

保留现有布局与样式，修正摘要内容，不新增天气或异常状态机制，不把效果预览直接用于伤害计算。

- 零次隐藏使用次数和空明细，不显示累计威力／连击零值。
- 当前计算值直接取已解算的威力、等级、速度与最终连击，手动威力标注来源；历史记录不再次叠加。
- 折射水、火、冰、毒、幽、恶系尚未参与结算的效果明确标注仅记录。
- 小程序能力状态与折射次数、来源、仅记录效果共同保存，避免重开后只剩增益而丢失记录。

## 回归触发条件

修改使用摘要、手动参数、战斗状态保存时，必须检查以下情景：

1. 从未使用：没有零次累计行、没有空明细，预览不增加伤害。
2. 携带光、电、水技能并实际触发折射：魔攻与速度按当前状态展示；水系能耗只记录。
3. 手动改威力及能力等级：立即采用当前值，次数不增加，不再叠加历史威力。
4. 重新打开小程序：次数、来源与仅记录状态不丢失。
5. 单技能、四技能、精简模式及 320px 窄屏、亮暗主题，文本不横向溢出。
6. 按用户最新要求移除增益明细折叠入口及展开正文；保留当前计算、使用后效果和仅记录状态，不重复手动覆盖提示或长篇来源规则。已按最终参考图实施，未使用与使用后均无明细入口。
7. 未使用技能必须实测空白间距：无参数时不渲染空控件行；清除摘要段落默认外边距及容器重复间距，无内容时不保留空摘要容器。使用前后与手动调整分别截图，不得只检查零值文字消失。
8. 视觉验收以用户最新两张参考图为准（剪贴板文件尾缀 0fc9c23b-c6c8-48e8-99c6-2fc710b9ee90、94b71211-8f36-4a4a-b262-9f24740c664e），不采用此前灰底分块方案。技能选择行与描述保持原样；未使用只接一行“本次可得”，已使用再增加一行紧凑的“累计状态：已使用×N 增益：…”；不增加灰底面板、独立当前计算段、折叠入口或空白占位。其他实际加成并入摘要，仍需区分当前生效与历史累计，不倒推或伪造历史来源。实施后必须按相同状态并排比图，检查行数、背景、行距和文字层级。

## 验证入口

- 核心状态：`tests/state/battle-activation.test.js`。
- 网页：`tests/ui/skill-usage-summary.test.jsx`、`tests/ui/skill-editors.test.jsx`、`tests/ui/app-integration.test.jsx`。
- 浏览器交互：`tests/e2e/uiux-skills-traits.spec.js`。
- 小程序：`miniapp/tests/skill-usage-panel.test.jsx`、`miniapp/tests/persistence.test.js`。
- 本机原生实测脚本、报告与截图：`output/playwright/usage-native-check.mjs`、`usage-native-report.json`、`usage-native-*.png`。
- 本机网页截图：`output/playwright/usage-zero.png`、`usage-active-desktop.png`、`usage-active-320.png`、`usage-active-320-dark.png`、`usage-manual-desktop.png`。

当前核心基线为 `5d5f3d070755e46343047741b4d30982c89ed14f`；网页／桌面源码版本 2.0.2，小程序源码版本 1.1.16。生产构建不代表已上传或公开发布，桌面安装包未重打。

## 紧凑参考图落地验收

- Web 单测 2018 项、小程序单测 458 项通过；lint 与两端生产构建通过。
- 浏览器回归首轮 116/120；更新手动提示的旧断言后，4 项单独复跑全部通过（其余 3 项为高并发下入口等待超时）。
- Web 已实拍未使用、使用两次、手动威力、320px 亮暗主题，并与参考图并排核对。与参考图不同的具体伤害和加成来自测试配置不同，未改伤害算法。
- 小程序 H5 已实际触发折射并验证一条累计摘要、无明细入口和无溢出；生产主包 1462567 字节。原生自动化端口 9420 拒绝连接，原生手机及 iPad 本轮未验收，不能将 H5 截图称为原生截图。
- 回归新增无内容不渲染容器、摘要不重复手动说明、四技能预览必须位于描述下方且间距为 0—6px、预览背景透明等断言。最终短预览实测间距 4px，补修后 13 项技能浏览器用例全部通过。
- 本机截图：`output/playwright/compact-usage-{zero,used,manual,320,320-dark}.png`、`reference-usage-comparison.png`、`mini-compact-usage-{zero,used}.png`。旧原生截图不属于本轮证据。
