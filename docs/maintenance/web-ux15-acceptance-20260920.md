# Web UX15 三项实施验收

2026-09-20，版本保持 2.2.2。用户已确认 `artifacts/web-ux15/preview.html` 的三项方案。本轮只做这三项，本地提交，不推送或发布。

## 改动

1. **W15-01 技能区对齐**：具体版双列使用共享网格行，双方特性及生命条件区按内容高度取齐，技能表头共同起点；不写死原来的 41px。1080px 及以下恢复原来的连续文档布局，没有占位空白。不改计算、技能配置或窄屏单方切换。
2. **W15-02 折叠条件摘要**：显示非默认天气、减伤、最终倍率、血脉魔法、印记及已启用负面状态；最多前两项和 `+N`，全默认无摘要，展开后隐藏摘要。悬浮提示与无障碍描述保留完整配置，并注明是否生效以计算结果为准。不重复能力等级；未使用的血脉魔法明确标注“未使用”。
3. **W15-03 设置窗交互**：顶部增加关闭按钮（桌面 36px、窄屏 44px）；复用工作区现有弹层焦点/滚动控制，支持 Escape、双向 Tab 循环，关闭后返回原入口或菜单按钮。保留即时设置与底部“完成”，无新增确认流程。

## 实测与对照

- 主视口 1920×1080，武斗酷猫对迪莫、具体版四技能；亮暗主题截图已读回。方案与实现使用同状态裁剪并合成对照，布局、字体、主题色与按钮位置一致。截图时通过既有拖动交互移开挡住裁剪区域的撤回按钮，随后恢复原位置，未改它的产品逻辑。
- 表头顶边从相差 41px 变为 **694.28125 / 694.28125px**。临时放大条件字体并增加 75px 条件高度后为 **728.28125 / 728.28125px**，移除探针后恢复原值；证明不是固定补白。
- 1080 / 390 / 320px 具体版堆叠布局：条件末尾到技能列表的额外空白均为 **0px**。390 / 320px 精简版设置窗分别宽 350 / 280px，左右安全边距 20px；顶部关闭入口 44×44px，位于可见区域。
- 摘要：默认 0 条；单项为“雨天”；三项为“雨天 · 减伤 20% +1”。测试配置后已恢复无天气、0 减伤、1 倍率。
- 设置窗：初始焦点“关闭显示设置” → Shift+Tab 到“完成” → Tab 回顶部关闭；Esc 关闭后焦点“打开菜单”。顶部点击关闭同样通过。打开时 body overflow 为 hidden，关闭后恢复原值。组件测试另覆盖背景关闭、完成、即时修改与原有 hidden / clip 滚动锁恢复。
- 开发页 4191 完成截图；生产预览 4197 复核可见四技能与设置窗，脚本资源 `/assets/index-COu2Osx7.js`，表头同为 694.28125px，Esc 和菜单焦点回归通过。早期生产探针在单技能页测到隐藏列表的 0px，不作为通过证据；改为显式切换四技能并等待可见后重新验证。

验收页：`artifacts/web-ux15/acceptance.html`。主截图 `actual-w1-after.png`、`actual-w2-after.png`、`actual-w3-after.png`；暗色 `actual-w1-dark.png`、`actual-w3-dark.png`；窄屏 `actual-settings-320.png`、`actual-settings-390.png`。方案 / 实现合成图为 `fidelity-w1.png`、`fidelity-w2.png`、`fidelity-w3.png`，已并排读回。

## 自动检查

- `npm test`：104 文件、1791 项通过（发布核心集，不是所有历史测试）。
- 两个相关组件文件：105 项通过，新增 6 项针对摘要和设置窗交互；未扩充大量重复测试。
- `npm run e2e`：11 项通过，包含生产构建与预算检查。
- `npm run lint`、`git diff --check` 通过。
- 最终颜色微调后重新构建并复核预算：JS gzip **347.61 / 348 KiB**，CSS gzip **55.70 / 56 KiB**；仅已有软预算警告，硬预算通过，未调预算。
- 验收文档与页面通过 UTF-8 / 中文输出门禁。

## 保留与边界

W15-04 设置说明精简、W15-05 结果行选中态、W15-06 320px 经典滚动条超宽继续留池，未擅自处理。未重跑全站体验巡检；没有宣称所有窄屏溢出已解决。未打桌面安装包、未验收小程序原生运行、未上传或发布线上。后续继续从待办池选择最多三项，耗尽再补巡检。
