# 优化轮 12：小程序深色可读性

用户已确认三项方案，本提交为第一批 M1；小程序版本保持 1.1.19。

## 改动与验证

- 技能结果行显式使用主题前景色，修复深色模式黑色技能名。
- 速度榜显示/隐藏文字按钮沿用原 PNG，在暗色媒体查询中对原生 image / H5 容器做亮色滤镜；只作用一层，避免嵌套滤镜叠加。
- 新增 CSS 契约回归；小程序 42 个测试文件、492 项全部通过。
- H5 实测 320/390/768px × 亮暗六组，结果选择、头像文字切换、主题前景色和图标滤镜通过，页面无横向溢出。已读回实际截图。
- `npm run miniapp:build` 开发构建通过，当前工作树/索引/HEAD 权威核心镜像一致。

## 明确未通过的验收项

- `miniapp:build:prod` 前置门禁失败：历史声明版本 `ec57cbcb9d2faae040d9ca81dfc4f8e5e4298e42` 有 7 个核心文件 release-content-mismatch。当前核心镜像一致，本次只改样式和 CSS 回归，未修改历史发布记录或放宽门禁。
- 原生自动化启动返回 `APPID_ERROR: 需要重新登录`。未自动操作认证窗口，已请用户扫码登录；本轮没有原生截图，不将 H5 结果当作微信原生验收。

## 证据与边界

已确认参考：`artifacts/mini-ux6-review-20260919/mock-m1-dark-after.png`、`mock-m1-icon-dark-after.png`。

实际截图和测量：`artifacts/mini-ux6-review-20260919/implemented/`，包含 `h5-qa.json`、手机和平板亮暗截图。正式样式与参考保持颜色、行高和图标尺寸，不增加装饰。

本地同版本提交；未上传、未提审、未发布。待登录后补原生核对，生产发布前另行处理历史核心门禁。
