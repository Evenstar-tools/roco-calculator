# Web UX19 小优化方案

状态：用户在本方案后回复“处理”，两项均已确认并本地实施验收，未发布。下文保留原方案与模拟阶段记录；最终结果见 `web-ux19-acceptance-20260920.md`。

## 范围与证据

2026-09-20，Web 2.2.2，基线 ccdfcb8 加 W18-02 标题字号修复。主视口 1920×1080、具体版，银月狼王对布灵布灵，四技能中选离魂术。主结果和四技能列表均制作亮暗原图；390×844 只补查非伤害底栏状态。没有检查全站弹窗、小程序或安装包。

现有候选已完成，定向补池得到以下两项，不为凑三项扩展范围。

## ① W19-01 非伤害状态

- 桌面主结果：红色“非伤害技能”改用中性色，不将正常的非伤害技能呈现为错误。
- 窄屏伴随修正：已选非伤害技能时，底栏“待输入”改为“非伤害”，保留单行横排。
- 实施必须按明确的非伤害原因分支；缺输入、缺条件和真实伤害比例各保留原语义及风险色，不改变计算。
- 确认后在已有结果测试中覆盖非伤害／正常伤害／输入不全，不增加重复全量检查。

## ② W19-02 四技能结果可读性

- 技能名、伤害、占比由 11px 调为 12px；表头由 10px 调为 11px。
- 保留 38px 行高、选中细边线、比例条及风险色，不扩大右栏或改宽屏布局。
- 确认后检查亮暗主题、长技能名及大数值，窄屏抽屉仅做受影响兼容验证。

## 模拟与还原

脚本 `artifacts/web-ux19/capture-candidates.js` 每项通过 try/finally 删除临时样式，底栏恢复原文字“待输入”；读回列表原字号为 11px。只有被用户明确要求的 W18-02 已修改产品代码。

对比图：`artifacts/web-ux19/proposal/mockups-combined.png`；可打开页：`artifacts/web-ux19/proposal/preview.html`。①含一个窄屏兼容子图，总计两项候选，不是三项。

W15-05 已有亮／暗 3px 选中线，排除重复提案。工具箱仅检查入口结构，不推荐无明确收益的改动。全部发现保留在既有待办池，下轮优先处理本表未完成项，不重新巡检。
