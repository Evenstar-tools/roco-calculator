# Web UX16 实施验收

2026-09-20，基线 `986ba35`，版本保持 2.2.2。本批提交主题 `fix(web): 完成 UX16 优化并补全窄屏精灵名`；只本地提交，不推送或发布。

## 范围与实现

- W15-04：显示设置默认保留当前模式一句简述，完整原文收进原生 details；切换静态/显示威力同步简述，支持键盘展开，保留已有关闭与焦点行为。没有改伤害算法或威力口径。
- W15-05：技能结果当前行增加 3px 内侧主题色边线，亮色紫、暗色金；原有伤害颜色、文字和行高不变。
- W15-06：移除 html/body 强制 320px 最小宽度；≤360px 压缩头部间距及图标按钮，头像 42→36px、列间距 4px，保留赛季名称和卡片四字名称。沿用旧的 overflow-x 规则，没有增加隐藏溢出作为修复。
- 用户新增 W16-01：窄屏选择框未展开时，用可换行的名称展示层覆盖单行输入显示，隐藏多余搜索图标以让出空间；名称字号仍为 13px。点击时回到原输入与全选行为；清空、搜索、候选选择、取消不另建逻辑。展示层 aria-hidden，辅助技术仍使用原 combobox 的名称和值。宽屏展示层 display:none，维持旧样式。范围是用户截图中的选择框；下方卡片对超长形态名原有的两行省略保持不变。

## 运行时验收

开发页 4191 与生产预览 4197 均执行定向验证；最终截图来自生产预览，资源 `/assets/index-D2CX7HD2.js`。

1. 1920×1080：亮暗主题中选中“抓挠”再切“棘突”，边线正确跟随，行高都是 38px。设置窗高 711.34→666.56px，约减少45px；简述与完整原文共存，Enter 展开、Esc 关闭、返回菜单焦点通过。
2. 390px：精简/具体版 clientWidth = scrollWidth = 375px；360px 均为345px；320px 均为305px。经典滚动条15px不再产生横向超宽。
3. 窄屏未展开名称的可用宽度为96 / 85 / 65px，“武斗酷猫”均一行18px，scrollWidth不超出clientWidth。点击后 selectionStart/End 为0/4，Backspace 后空值并恢复16个默认候选，Escape恢复原选择。
4. 长名称“石冠王蜥（球球尾巴的样子）”在320px选择框完整换行，宽65px、高72px，无横向裁切。后缀不删除；代价是极长名称会增高该选择框，不强制缩字或省略。该截图下方的卡片摘要仍按原有两行省略，并非本次新增裁切。
5. 320px普通候选下拉 left/right = 16/304，可用宽305；菜单 left/right = 81/291，不越界。暗色名称和设置窗已截图读回。1920px名称展示层隐藏，确认宽屏不改。
6. 定向脚本第一次在窄屏按桌面按钮名找“具体版”而超时；改用窄屏“切换到具体版”语义定位后，在开发/生产预览完整重跑通过。失败轮不作为验收依据。

## 自动检查

- `npm test`：104文件、1792项核心发布测试通过，不冒充全部历史用例。
- 5个相关组件测试文件：70项通过；新增完整名称展示与全选/删除/取消流程、说明折叠与模式简述2项。已有 NEW 标签断言改为准确定位卡片标题，避免新展示层的同名文本造成歧义。
- `npm run e2e`：11项通过，含生产构建与预算门禁。
- `npm run lint`、`git diff --check`、UTF-8严格解码及交付中文门禁通过。
- JS gzip 347.71 KiB < 348 KiB；CSS gzip 55.89 KiB < 56 KiB，未调整硬预算。仍接近上限，后续新增内容需继续检查。

## 视觉证据与后续

验收页 `artifacts/web-ux16/acceptance.html`。选择框 `actual-320.png`、`actual-long-name-320.png`、`actual-name-dark.png`；设置 `actual-w1-after.png`、`actual-w1-expanded.png`、`actual-settings-dark.png`；结果行 `actual-w2-light.png` / `actual-w2-dark.png`。`fidelity-settings.png` 和 `fidelity-result.png` 已同图对照方案与实现，布局差异在2px以内；新增完整名称显示是本轮用户明确追加的变化。

原待办池6项及追加名称项现已处理完，下轮需要时再做定向巡检补池。本轮未做全站重新发现、未改小程序、未打桌面包、未更新线上。桌面浏览器窄视口证据不等同手机真机验收。
