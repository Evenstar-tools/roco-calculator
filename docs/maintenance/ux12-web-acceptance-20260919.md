# 优化轮 12：已确认方案实施与同版本验收

日期：2026-09-19。Web 版本保持 2.2.1，小程序保持 1.1.19；仅本地提交，不推送、不打包发布。

## 实施范围

用户确认全部三项候选，并追加要求“同种类字体大小保持一致”。先完成小程序 M1 独立提交 `89c4575`，再实施本 Web 批次。

- 保存摘要：成员入口写明“已保存配置”；从计算器双方入口进入仍称“当前配置”，避免误称队伍已保存数据。
- 字体一致性：四项指标的标签均为 12px/400/1.4，数值均为 18px/700/1.3。宽屏同一排；窄屏统一两列、标签在上数值在下，不再给物理/魔法耐久另设小字横排。
- 试算状态：手动微调后明确显示“试算草稿 · 尚未应用到成员”，速度线、加速区域与速度一览同步标为“试算”。计算器来源提示“尚未应用回计算器”。摘要与保存值仍保持原值，主动应用之后才更新。
- 速度线识别：本体复用原精灵头像、保留实心点；目标采用同色空心点。节点排序、横向拖动、键盘激活、目标选择和原主题颜色不改。

未修改计算公式、核心数据、持久化流程、依赖、版本号和性能预算。design-craft 用于约束字体层级、沿用现有视觉语言，并对照已确认图片验收，不新增装饰。

## 自动化与实际界面

- 定向单测：34 项通过；新增回归覆盖保存摘要与试算分离、本体头像及速度一览状态。
- Web 全量单测：174 文件、2638 项通过。
- Lint：通过。
- 浏览器回归：194 项覆盖全部通过。首轮 186 项通过，8 个速度线用例受新增测试错误预期影响；修正后定向复跑 8 项全部通过（再次经过生产构建），没有跳过或放宽断言。
- 生产构建：浏览器套件启动前完整执行 `npm run build`，验收、当前核心一致性、资源绑定和性能硬门禁通过。
- 实际截图：320/390/1440/3840px × 亮暗，共八组；指标字号、字重、行高一致；本体头像加载成功；试算不修改摘要和本地已保存成员；速度一览状态一致；无页面/队伍弹层横向溢出，无页面脚本错误。
- 维护回归：摘要测试覆盖 320/390/700/1024/1424px × 亮暗，检查文字完整、同类字号一致和试算后保存值不变；速度线用例覆盖 4K、拖动后的首次键盘激活、保存/试算状态及实心/空心标记。

首轮浏览器回归暴露新测试预期错误：把“沉默”误当成减速性格。真实配置为加生命、减物攻，速度仍是 192；已修正测试预期，不修改计算逻辑。

## 对图记录

参考：`artifacts/web-ux12-review-20260919/mock-w1-light-after.png`、`mock-w2-light-after.png` 及对应暗色版本。状态为音速犬、普通性格、生命/物防/魔防各 60；试算改胆小，速度从 192 变为 220。

实测：`artifacts/web-ux12-review-20260919/implemented/`。

- `web-qa.json`：八组字体、标记颜色、保存边界及溢出测量。
- `reference-speed-comparison.png`：已确认方案与正式速度线同框对比；头像、节点、轴线与主题色一致。搜索框清除按钮因指针悬停状态可见，不是本次新增控件。
- `reference-draft-comparison.png`：已确认方案与正式摘要/草稿同框对比。唯一结构差异是用户追加确认的字体统一：窄屏第二排从横排小字变成相同大小的上下结构，摘要高度相应增加约 30px。
- `comparison.png` / `index.html`：修改前实测与本次正式代码实测的综合对比，非模拟落地。
- 已读回 320px 暗色、390px 亮暗对比、1440px 整屏、4K 摘要及小程序两项深色参考对比。

## 剩余边界

- 小程序 M1 已通过 492 项测试、六组 H5 亮暗手机/平板检查及开发模式 WeApp 构建；详细记录见 `ux12-mini-acceptance-20260919.md`。
- 小程序原生工具仍需重新登录，未取得原生截图；生产构建被历史声明版本核心差异门禁阻挡。用户回复“继续”后推进 Web，不将其当作登录成功，不绕过历史门禁。
- Web 包体仍有软警告：CSS gzip 54.61 KiB / 硬上限 55 KiB，JS gzip 343.08 KiB / 344 KiB，客户端 15.79 MiB / 16.50 MiB。门禁未放宽，本轮不扩展包体重构。
- 工作区原有未跟踪文档、夹具及其他文件保持不动，不纳入本批提交。
