# 二级面板六项微调验收

2026-09-10。用户确认六项全部实施；基线提交 b5dddc6。本批仅 Web / 桌面共用界面，不推送、不发布、不打安装包。

## 范围与参考

参考：`artifacts/web-ux7-secondary/mockups-combined.png`，分项 `w1` 到 `w6` 的 1440 / 390 / 320px 亮暗主题 after 图。实际截图由 `tests/e2e/secondary-panels-polish.spec.js` 输出到同目录 `actual-*.png`。

采用 Design Craft：保持已有字号、资产和色彩体系，按已确认图核对布局和颜色；不把新手引导、分享或清理确认弹窗纳入通用选择器。

| 项 | 实施与尺度 | 行为边界 |
| --- | --- | --- |
| ① 队伍 | 空态宽度最多460px；桌面内边距30×22、手机24×16；操作44px高；空态前移，页签背景统一 | 导入接既有 navigate 和 exchangeMode；无队伍隐藏不可用工具，有队伍恢复；不改分析结构 |
| ② 技能 | 卡片9px圆角、浅底白卡；页签背景与选中态；学习更新标题下距8px | 不改字号、头像、家族聚合、双向跳转和手机紧凑工具区 |
| ③ 设置 | 勾选框22px置于44px列；说明与开关同排；取消重复卡框 | 原设置状态和存储通道不变，保留完整威力说明 |
| ④ 配置 | 数字18px、等宽数值；统计行6×10px；确认操作强调 | 空库主操作为导入，可导出时强调导出，禁用逻辑不变 |
| ⑤ 关于及子页 | 指定弹窗12px圆角、标题分隔与底部12px间距；许可减少套框，版本条目分隔 | 许可正文、链接、反馈入口、返回/关闭和历史内容保留 |
| ⑥ 获取应用 | 安装包入口前置并强调，二维码大小不变 | 使用实际 DOM 顺序同步键盘阅读顺序，原 href 完全保留 |

暗色金底主按钮使用深色字，不删除键盘焦点圈。预览中空态导入为DOM示意，正式实现使用 React 事件，不保留复制DOM脚本。

## 验证

新增单测：空队伍导入、返回、新建后恢复工具状态。

新增浏览器检查：六项 × 三宽度 × 亮暗主题；设置勾选同行、状态保存；安装链接顺序与地址；关闭恢复焦点；常用配置确认按钮；版本记录、鸣谢、免责声明、反馈、新功能和赛季页逐页截图。

额外端到端场景使用已有阵容fixture解析六人24技能，显式确认个体配置后导入，检查成员编辑、分析、对位和导出入口。

保留上一轮手机密度门禁（1 / 4项已选；650 / 700高度），不以截图通过替代完整交互回归。

### 最终结果

- 全量单测：113 个文件、1864 项通过；新增队伍入口单测覆盖导入、返回和创建。
- ESLint 通过；生产构建、16 项验收矩阵、核心镜像一致性及622只精灵资产绑定检查通过。
- 全量浏览器回归：89 项通过。使用项目 Playwright 配置的临时入口仅将服务启动改为 preview:test，生产构建已单独执行；测试集未裁剪。
- 首轮发现320×800热门配置展开弹窗高度766px，超过760px安全范围；为配置弹窗增加视口高度上限及纵向滚动，最终全量回归通过。未放宽原断言。
- 资源硬门禁通过：总量13.54MiB（硬限14）、CSS gzip 41.15KiB（硬限46）、JS gzip 254.48KiB（硬限256）。仍有软预算警告，本轮未更改预算。
- 本地4175首页和新版样式均返回200，首页实际引用 index-BKEYsl0G.css，内容包含新增面板样式。生产主入口为 index-CYxPRkbw.js。
- 修改文件严格UTF-8可解码，无替换字符或空字节；按仓库.gitattributes使用LF。git diff --check通过。

### 截图读回与差异

六项实际截图覆盖1440、390和320宽度的亮暗主题；输出 `actual-overview.png` 实测总览。使用 Design Craft 对照脚本并排比较 `verify-w1.png`、`verify-w2-mobile.png`、`verify-w3-desktop.png`、`verify-w3-mobile.png`、`verify-w4-mobile.png`、`verify-w5-mobile.png`、`verify-w6-mobile.png`，已读回检查。

修正了配置检查条因选择器优先级造成的8px高度偏差，恢复确认图中的内边距；设置勾选、页签、卡片和获取应用布局与参考一致。额外读回320暗色设置及真实六人阵容导入后的成员面板。原精灵图、技能图及二维码未替换、缩小或重绘。

有意差异：队伍空态导入使用既有图标库图标和React事件；获取应用从CSS视觉重排改为真实DOM顺序；配置弹窗在低高度视口中可滚动。对照图遮罩背后的计算器内容及撤销状态因测试上下文不同，不作为面板视觉差异。

本批验证范围为Web生产构建和浏览器运行时，复用桌面界面源码；未重打桌面安装包，未做小程序原生验证或发布。所有测试使用独立浏览器，不触碰用户当前配置；`draft_189d29c5_folder/`保持不动。仅提交本批九个文件，不推送远端。
