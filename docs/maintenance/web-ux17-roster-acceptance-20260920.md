# UX17 成员侧栏恢复验收

## 范围与实现

用户明确要求恢复此前提供的图 3，不新增顶部操作按钮（W17-01 仍待确认）。基线为 `2b2ebd5`，版本保持 2.2.2；本轮只做本地恢复，不推送或发布。

产品改动仅为删除 `TeamDrawer` 在能力分析页传入的 `data-compact-roster` 和 `compact` 两个开关，复用成员配置页已有布局。技能图标、每行攻防按钮恢复，底部“当前成员操作”不再渲染；不修改能力计算或成员数据。

## 验收

- 新增切页回归测试：修复前因 `data-compact-roster=true` 失败，修复后通过；检查侧栏 DOM 不变、没有重复底部操作，以及攻／防按钮仍传递正确成员。
- 队伍 UI 测试 29 项、关键测试 1792 项、E2E 11 项通过；lint 通过。E2E 已含生产构建和体积门禁，不重复构建。
- 1920/1440 宽屏两页侧栏均为 280px，卡片均为 262×96px；390 窄屏均为 174×147px，320 窄屏均为 139×147px。四个尺寸切页前后的全部测量值一致；6 组技能图标和 12 个攻防按钮保留。
- 1920 深浅色及 390 窄屏截图已读回。按 design-craft 以原有配置页为参照，对照两页实际侧栏，不引入新的样式。
- 本地静态验收页有既存的 `/favicon.ico` 404，不影响产品交互；未将其计为产品运行错误。

## 本地证据

- `artifacts/web-ux17/roster-acceptance.html`：并排实测对照。
- `artifacts/web-ux17/roster-comparison.png`：配置页／能力页侧栏合成图。
- `artifacts/web-ux17/roster-full-1920.png`、`roster-dark-1920.png`、`roster-390.png`：真实页面截图。
- `artifacts/web-ux17/verify-roster.js`：四尺寸切页测量与截图脚本；仅在隔离的验收浏览器上下文中使用测试队伍。
