# Web 2.2.2 发布验证

日期：2026-09-19。按用户“2.2.2 提交看 Web”发布当前已验收的同族形态与界面调整。本记录接续同族形态本地验收，不改变那份历史记录的发布状态。

## 发布内容

- 同族形态切换保留本场威力、能力、特性层数和其余配置；三三等价特性直接继承，支持撤回与分享，不覆盖个人预设。
- 合并此前四个本地提交：速度线横向全宽及键盘交互、配置摘要统一字号、窄屏赛季标题，以及小程序暗色文字对比度。
- 根版本、锁文件、预设版本、离线缓存及用户更新记录统一到 2.2.2。
- 保留既有二维码个体解析修复；二维码只编码分配方向，导入时各项按 60 初始化，不宣称读到原始数值。

## 发布前验证

| 项目 | 结果 |
| --- | --- |
| Web 单元测试 | 176 文件 / 2,653 项通过 |
| 浏览器回归 | 194 项通过，含二维码真实图片导入、旧队伍恢复、320px 至 4K 适配 |
| ESLint | 通过 |
| 数据检查 | 622 精灵 / 581 技能，无重复或种族值不一致 |
| 正式构建 | 通过，原有阻断门槛未放宽 |
| JS gzip | 343.76 KiB，低于 344 KiB 阻断线 |
| CSS gzip | 54.67 KiB，低于 55 KiB 阻断线；保留原有警告 |
| 当前共享核心 | 67 个镜像文件同步；构建前检查通过 |
| 小程序发布前配置检查 | preflight 通过，不等于原生界面验收或提审 |

本功能小程序源码的 496 项单元测试、H5 与微信 development 构建、5 档兼容性启动检查已在前一轮完成，详见同族形态本地验收。本轮不把它们计入 Web 浏览器测试数量。

## 发布边界与线上验收

本轮推送主分支触发 Web 自动部署；不创建桌面安装包，不上传或提审小程序。小程序版本仍为 1.1.19，历史发布核心差异和原生界面验收维持独立待办，不绕过生产门禁。

推送后核对线上 index、缓存脚本、运行时数据及全部 JS/CSS 的哈希，并在真实线上浏览器复验同族切换和二维码个体导入。最终线上证据存于项目本地 `artifacts/release-2.2.2/`；提交成功本身不代表部署已通过。

## 远端检查补齐

首轮提交 `ac1bbb3` 已触发自动部署，2026-09-20 00:02（北京时间）24 个线上文件与本地构建哈希全部一致。远端小程序测试同时检出内置预设的来源版本仍为 2.2.1；已使用现有生成器重建 JSON 与压缩载荷，并将生成及定向测试步骤补入发布检查单。没有修改断言或小程序独立版本。
