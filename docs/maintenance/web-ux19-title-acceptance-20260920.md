# W18-02 标题字号修复验收

2026-09-20；Web 2.2.2；用户确认“修复字的问题”。仅本地修复验收，未推送、未发布。

## 变更

基线 ccdfcb8：470px 竖屏，精简标题 14px，具体标题 15px。仅在现有不超过 620px 的竖屏媒体查询中，将非工具页标题统一为 14px；宽屏规则不改，工具页不受本条覆盖。

现有模式按钮 E2E 同时比较标题字号、字重和行高，防止再次只验按钮而漏验整个标题栏；没有新增重复测试文件。

## 验证

- 320、390、470、620px，亮暗主题，精简／具体共 16 组：标题均 14px、字重 800、行高 normal；1920×1080 标题仍为 18px。
- 470px 两主题、两模式实际截图已读回：字形大小一致，无新增裁切。
- npm test：104 文件、1792 项通过；独立页头测试 7 项通过；lint 通过。
- npm run e2e：生产构建及 12 项 E2E 通过。E2E 已包含构建，不重复构建。
- gzip 预算：JS 347.96 / 348 KiB，CSS 55.84 / 56 KiB，通过但余量很小；UX19 实施时仍需检查预算。

脚本：`artifacts/web-ux19/verify-title.js`。截图：`title-light-0.png`／`title-light-1.png`／`title-dark-0.png`／`title-dark-1.png`，均位于同目录。对照页 `artifacts/web-ux19/title-acceptance/preview.html`，左为具体版、右为精简版，均是修复后实测，不是注入模拟。

本轮另外两项 UX19 仅做方案出图，未实施。小程序、桌面安装包和线上发布不在本次验证范围。
