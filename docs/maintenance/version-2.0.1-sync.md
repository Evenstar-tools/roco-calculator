# 三端版本同步验收

日期：2026-09-11。范围是版本递增、源代码提交推送及本地产物归档；不包含网站部署、GitHub Release 发布或微信上传审核。

## 版本与改动

- Web / Windows：2.0.0 → 2.0.1；小程序：1.1.14 → 1.1.15。
- 同步 package、lock、展示版本、离线缓存、用户更新日志及 226 条预设库的版本元数据。
- 本批包含独立速度 / 耐久榜、倍率多选、原入口保留、小程序榜单入口与结果栏修复，以及已确认的四只精灵预设。
- 共享核心继续锁定 `ca2d07f2abd435e35c6cc6648e9f333865e95da9`；60 个共享文件与当前源码一致。版本标签与核心内容修订各司其职，不伪造核心变动。
- 修正更新日志测试对旧标题和旧版本入口的硬编码；仍校验当前版本、内容非空、短标题唯一及新功能数量上限。

## 本地验收

- Web 单元 / 集成：116 个测试文件、1,892 项通过；小程序：38 个文件、439 项通过。
- 数据、素材绑定、共享核心、发布前置、ESLint、差异空白检查通过。
- Web 生产构建通过；JS gzip 260.68 KiB（硬上限 272），JS 原始 879.95 KiB（硬上限 880），CSS gzip 43.89 KiB（硬上限 46）。原始 JS 余量很小，后续功能必须重新检查，不能再靠忽略警告放行。
- 浏览器端完整回归：`npm run e2e -- --workers=2`，111 / 111 通过。首次 10 workers 同时打包时出现 3 项入口点击超时、108 项通过；降低并发后完整重跑均通过，不跳过用例、不放宽断言。首次时序失败仍记录在此，不据此宣称已彻底消除高负载下的偶发性。
- 小程序生产包 1,401,502 字节，门禁通过；产物清单 SHA256：`b65d8c34d0180b5ee8a2648c31514f05ba90d54bcbb799fff83a4521d4487186`。
- 新生产构建在微信开发者工具手机模拟器验证两个菜单入口对齐、箭头、跳转和返回，并读回截图。截图：`output/playwright/menu-layout-native.png`。这不是手机实机验收。
- Windows 安装包生成通过，产品名称及 ProductVersion / FileVersion 均核对为洛克计算器 2.0.1；桌面暂存与打包资源检查 581 个离线技能图标。此轮未执行全新安装、卸载及重装。
- Web 与桌面打包资源中的预设库均回读为 2.0.1、226 条；两个 ZIP 全量读取无错误。

## 本地归档

目录：`installers/v2.0.1/`（产物不入 Git）。

| 产物 | SHA256 |
| --- | --- |
| 洛克计算器-2.0.1.exe | b23a5e57ae355f2d9494535e1ab9fe14f6079cb058c55a582be451abc514ad3d |
| rock-calculator-webapp-2.0.1.zip | 028234a31ab5c4dc2619df4f31be7cbfab9aa8bb9ffc165191d7b42616ab653f |
| rock-calculator-miniapp-1.1.15.zip | 0cc6842e6c08bfb9ef7a63d2a12d835932d26faa92fcea93a571a779ed846129 |

同目录 `SHA256SUMS.txt` 记录三份哈希。ZIP 与校验文件仅保留本地；若后续要求 GitHub Release，只上传版本化 EXE。

## 仓库管理与发布边界

- 唯一写入目标为 `Evenstar-tools/roco-calculator`；旧仓库保持只读。
- 推送前核对 `origin/main` 为开发分支祖先，计划快进本地 main、推送 main，并创建 Web / 桌面与小程序对应的版本标签；不强推、不改写旧标签。
- 保留 `draft_189d29c5_folder/` 未跟踪说明书草稿，以及其他工作树和分支；不使用大范围清理。
- 推送后单独回读 main、标签及 CI；本地通过不等于远端通过，源代码同步不等于正式发布。
