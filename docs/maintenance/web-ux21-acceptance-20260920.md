# Web UX21 第 2 项与累计修改发布验收

## 授权与范围

用户确认“2改 13不改 然后提交看web”：只实施预设清单别名搜索，开关文字与统计栏不改；恢复提交本会话前面暂留的已确认修改，沿用 2.2.2，仅推送 Web，不重发安装包或小程序。

W21-01／03 已在待办池记为用户否决，不反复提案。产品代码未注入模拟样式；只增加 fullName 与 aliases 的匹配，查询仍限制在 sourceEntries。大小写、首尾空白、清空、排序和导入策略不变。

## 当前验证

- 新增组件测试先在原代码复现失败，实施后通过。覆盖正式名、中文别名、英文别名大小写及空白、未收录精灵排除、空结果、清空焦点、导入按钮及原统计／开关名称。
- 23 项相关测试通过；104 文件 1806 项发布测试通过；12 项生产构建 E2E 通过；lint、共享核心镜像、数据绑定、验收矩阵与构建通过。
- 首轮发布测试发现暖阳新增 otherFireSkillUseCount 使控件语义审计组由 14 增至 15；已有 ID 唯一性断言通过，仅补齐预期组名，重跑发布测试全部通过。未削弱校验或改控件 ID 算法。
- 1920×1080、390×900、320×900，亮暗两主题实际搜索圣甲虫均命中圣凯布米龙；无横向溢出。无结果后清除恢复 230 项，焦点返回输入框。
- 第 1 项仍显示“仅预览配置项”，第 3 项仍显示“相同／不同／新增”。相关测试与实测均检查这两个排除条件。
- JS gzip 348.20 KiB（硬上限 348.25），CSS gzip 55.96 KiB（硬上限 56）；沿用已授权预算，基线警告仍保留。
- 真实截图已读回：`artifacts/web-ux21/actual-1920-light.png`、`actual-390-dark.png`、`actual-320-light.png`；验收合图 `artifacts/web-ux21/acceptance/preview.html`，脚本 `verify-local.js`。不把此前效果模拟当实际功能证明。

## 提交清单

本次同时收口本会话已确认的 UX20 三项、预设与收藏浏览、暖阳规则及圣甲虫别名、预设清单别名搜索和相关测试记录。此前 UX18／19 和加尔已有本地提交，随 main 一并推送。详细预设清单、覆盖审计见 `preset-coverage-20260920.md`，暖阳验证见 `warm-sun-alias-20260920.md`。

只逐文件暂存本任务文件；既有其他验收草稿、测试夹具和异常文件名不纳入。CHANGELOG 保留完整记录，网页用户日志汇总可感知变化；不改版本号。

## 发布边界

提交前此记录仅确认本地验收。后续顺序：提交→推送 origin/main→线上静态资源哈希与本地构建对比→独立浏览器确认别名、230 项预设与第 1／3 项原样。远端证据保存在 `artifacts/web-ux21/`；未经远端回读不宣称上线。

## 线上回查与 CI 补齐

- 主提交 `10ec91c` 已推送 origin/main。首次推送因默认账号无权限失败；随后仅在该命令使用已登录的 Evenstar-tools 授权，未切换日常默认账号，远端 SHA 回读一致。
- 2026-09-20 17:20（北京时间），线上首页、Service Worker、运行数据、两份预设数据及全部 JS/CSS 共 24 项 SHA-256 与本地发布构建一致；证据 `artifacts/web-ux21/live-hashes.json`。
- 独立线上浏览器加载 `index-DuZPxvEH.js`：圣甲虫命中圣凯布米龙，配招为暖阳／星火／热身／虫击；清空恢复 230 项且焦点正确，1920／390 实测，开关名称及三项统计保留。未点击导入，不修改用户在线配置。
- 首轮远端 Web validate 与 e2e 成功，小程序测试报 3 项预设数据一致性失败：Web 230 项，小程序内置副本仍 227 项。按原生成脚本同步 `common-spirit-config.json` 与压缩 payload，语义校验一致；这是提交必需的数据同步，不作小程序上传、提审或上线。
- 同步后小程序 43 文件、496 项本地测试全部通过；原有测试未放宽断言。
- 发布检查单增加“同版本预设修改也必须同步并运行一致性测试”的触发条件，避免只改版本号时才同步。后续补齐提交不改变已验收 Web 资源，不重复构建相同 Web 包。
