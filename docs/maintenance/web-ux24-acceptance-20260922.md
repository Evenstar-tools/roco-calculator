# Web UX24 验收：属性查询与电鹿入口

日期：2026-09-22
基线：`60a36f9`

## 已实施

- 属性查询文案改为“仅展示已确认最终形态”。
- 对应精灵卡片移除重复属性，仅保留阶段。
- 从主站进入电鹿页时，攻击方当前配置若不满足完整配置标准，则使用“常用配置”；完整配置仍按当前配置带入。
- 电鹿层数切换不再触发整套斩杀层数扫描，只从已算结果中切当前层。

## 实测

- 地＋武：8 个已确认最终形态；卡片仅显示“二阶”。
- 主站选择波普鹿后进入电鹿页：预设为 `standard`，速度增益性格处于选中状态。
- 层数 0→1：输入反馈约 62.7ms，结果层切换约 3.4ms。
- 1920px 无页面级横向溢出；亮暗主题实际截图位于 `artifacts/web-ux24/acceptance/`。

## 门禁

- `npm test`：112 文件、1917/1917。
- 电鹿专项：deer-entry + type-query 12/12；deer-critical 3/3；deer-page 29/29。
- `npm run lint`：通过。
- `npm run e2e`：13/13。
- `npm run build`：通过，性能预算通过；JS gzip 356.11 KiB，CSS gzip 57.59 KiB。
