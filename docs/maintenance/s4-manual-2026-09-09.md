# S4 手册数据本地候选与核验边界

## 本次提交范围与验收

用户在主开发任务明确要求恢复提交、推送并检查线上；下文暂停记录为此前阶段状态。本次提交 10 只新精灵的 463 条分类学习关系、拖拉机及其截图图标、两项待核保护和已批准的 14 MiB 总资源硬上限；不纳入其他界面或桌面改动，也不纳入麦芒实体。

独立工作树按实际提交文件验收：完整网页构建通过（13.06 MiB），7 个相关测试文件、45 项通过，小程序运行时 8 项通过，数据结构、ESLint 与差异检查通过。全量测试曾因停止指令中断，不将其计为通过。推送前线上仍为 579 技能、狼王 6 项；发布后需另行核对 580 技能、狼王 47 项与查询目录实际内容。此提交不打包、不发布桌面 Release。

最新状态：已批准的两处名称修正已随 `c09ead4` 推送；后续追加的“先 BWIKI 印证再处理”门槛不反向撤销该提交。本轮仅撤回尚未提交的麦芒实体和冰锋横扫零威力覆盖，不提交、推送、打包或发布。以下 BWIKI 结论来自来源核验任务回报，本开发任务未独立重复联网核验。

## 最新执行结果：两项保留、两项待核

用户指令先后已由来源任务澄清；本地数据、生成器及 reviewed evidence 按下表处理，不能依据历史测试结果扩大为 BWIKI 已确认。

| 项目 | 当前本地候选 | 待核状态 |
| --- | --- | --- |
| 麦芒 | 已移出 current 及 Web/小程序派生技能列表 | 候选参数仅保存在 pending；截图裁切素材保留，不建立正式实体或学习关系 |
| 冰锋横扫 | 恢复已提交基线威力 1 及原字段来源 | BWIKI oldid=40293 为威力 1、耗能 4；截图 0 仅保留为 pending 冲突记录 |
| 引力偏转 / 引力旋转 | 保留 c09ead4 的名称修正与稳定 ID | BWIKI 尚未收录；依据为用户已批准的截图，不是 BWIKI 确认 |
| 摇铃魔偶的午夜噪音 / 午夜爆音 | 保留 c09ead4 的学习关系、默认槽及预设修正 | 午夜噪音实体有 BWIKI 支持，摇铃魔偶学习关系尚无 BWIKI 支持 |

生成器导入会将两项放入 pending，应用阶段也会拒绝旧证据中的麦芒新增和冰锋零威力覆盖；本批已落地的覆盖可精确撤回，不改其他精灵和学习面。当前快照为 619 只精灵、580 个技能。回归测试覆盖旧证据重放及幂等；后续新 S4 候选需先获 BWIKI 印证。

验证：Web 数据及双向查询定向 5 个文件、24 项通过；小程序运行时 8 项通过；快照校验、UTF-8 输出门禁与 `git diff --check` 通过。直接回读确认冰锋横扫整个实体与 HEAD 完全一致、引力偏转实体与 HEAD 完全一致、午夜噪音学习关系和预设保留。当前摘要为 `682cd5d2668b9e51f3a242c92f01b56d2769f5194d88cf0095b178d959b88c58`，HEAD 仍为 c09ead4，暂存区为空。以下章节均为先前阶段历史记录，不代表本次最终状态；本表优先。

## 历史记录：两个独立技能与适度扩容

- 拖拉机：机械系、物攻、能耗 3、威力 85；描述为“造成物伤，位于1号位时，先手+1。传动1。”，稳定 ID 为 `skill_e76ee2ef859cb0dd`。
- 麦芒沿用 `skill_a439be4a746f684b`。两个技能均未绑定精灵，截图未提供对应精灵身份；技能的先手、传动及能量回复说明不额外套用到伤害倍率。
- 用户明确授权从原截图裁切、缩放，无 AI 重画。两个图标为 128×128 PNG，约 33 KiB/张，位于 `public/assets/skills/`；已视觉读回，仅保留技能原图，不包含文字或悬浮头像。此素材验收按 design-craft 的原色、统一尺寸和无多余内容原则执行。
- 图标重放脚本为 `scripts/bwiki/import-s4-manual-icons.mjs`，参数按麦芒、拖拉机顺序传入原图路径。脚本核对原图 SHA-256 和尺寸后再裁切。第 33 张补充图的文件名及哈希留在 `supplementalImages`，不影响原 32 张、463 条分类关系的统计。
- 客户端总量从原硬上限约 13.02 MiB 改为警告线 13.5 MiB、硬上限 14 MiB（约放宽 7.5%）。JS、CSS、运行时数据阈值不变；回归覆盖 14 MiB 边界和超出 1 字节的阻断。
- 新产物总量 13.10 MiB，网页构建与性能门禁通过；缺图定向检查通过。Web/桌面全量 105 个文件、1813 项通过；新增图标与阈值定向 12 项通过；小程序数据及技能定向 16 项通过（原 26 张本地图标断言已更新为 28，并逐一验证两张新图标地址）。lint、快照校验及 diff 检查通过。覆盖重放幂等，两技能的精灵绑定数均为 0。
- 当前快照为 619 只精灵、581 个技能；新的内容摘要为 `97450a5620a0a61bf230952c6df165e43c8b08b40a2d583b7008941af5136002`。
- 以下旧记录中的麦芒缺图和总体积阻塞已解决；两只精灵的旧预设引用仍未调整。本次不做发布、不变更版本号。

## 来源与范围

采用已完成三段复核的 32 张洛克手册截图提取结果。仓库留档为 `data/reviewed/s4-manual-2026-09-09.json`，包含图片序号、文件名及 SHA-256；不包含私人会话 ID、消息 ID 或本机路径。原图继续保留在来源任务的 `feishu-s4-extract/lark-im-resources`，不复制进公开资源目录。

正式图鉴号及学习面回读如下。分类数量是归属关系数量，不是默认四技能槽。

| 精灵 | 图鉴号 | 自学 | 血脉 | 技能石 |
| --- | ---: | ---: | ---: | ---: |
| 银月狼王 | 445 | 14 | 18 | 15 |
| 月使鹭纳 | 448 | 15 | 18 | 16 |
| 圣凯布米龙 | 451 | 13 | 18 | 13 |
| 智辉章脑 | 453 | 13 | 18 | 13 |
| 未完虫 | 454 | 15 | 18 | 15 |
| 玳塔 | 456 | 13 | 18 | 13 |
| 测风蝉 | 458 | 15 | 18 | 16 |
| 黑手浣熊 | 460 | 15 | 18 | 16 |
| 摇铃魔偶 | 462 | 13 | 18 | 14 |
| 布灵布灵 | 465 | 13 | 18 | 13 |

合计 463 条分类关系，241 个唯一技能名。保留全部精灵现有稳定 ID、种族值、头像、特性和进化链；只补图鉴号与学习面。未把单个字段已核实扩大为整个 S4 数据已正式核实。

## 关键字段和证据边界

- 引力旋转更名为引力偏转，保留 `skill_f7eea4de117d30ed`，没有新增重复实体或旧名并列。图 18 确认名称、幻系、耗能 2、无威力；描述和防御类别仍沿用原视频来源，不假装本次图片提供了这些字段。
- 冰锋横扫：冰系、耗能 4、威力由 1 改为 0。
- 摇铃魔偶：完整学习面和当前默认槽改用既有午夜噪音 `skill_15fb272dc50faf16`。旧午夜爆音实体仍留作历史占位，未删除。
- 智辉章脑：新学习面自学涌泉、技能石落雨，移除该精灵的水波术及降雨前瞻归属。没有进行全局技能合并，也没有推断两组名称是别名。当前默认槽仅保留仍合法的汇流、分光，未擅自为玩家选择替代技能。
- 麦芒：新增草系物攻、能耗 5、威力 105 及图片描述；没有精灵绑定。未推断能量回复的额外计算规则。
- 自学记录使用“默认学习（等级待确认）”；血脉使用“血脉（具体血脉待确认）”。未添加学习等级或具体血脉。
- 未推断图鉴 466—468 的问号名称，也未更动其他精灵。

## 重放与回退

无需重新访问外部网站即可重放：

```powershell
node scripts/bwiki/apply-s4-manual.mjs
npm run data:runtime
node scripts/miniapp/build-bundled-runtime.mjs
```

首次导入经复核 JSON 可使用 `--import <来源.json>`。正式覆盖函数具有幂等检查；已有 `meta.s4Manual` 的工作区重跑前瞻 CLI，会在前瞻生成后重放本批较新的覆盖。若从更早原始快照完整重建，必须按“原始生成 → 前瞻覆盖 → 本手册覆盖 → 运行时生成”的顺序执行。

本轮只改动数据、专用覆盖脚本、相关回归测试及本说明；没有提交。回退时只处理本批文件和前瞻 CLI / 测试内的本批改动，不可整体 reset 工作区。开始时已有的 27 个脏文件及离线技能图标工作均未覆盖。

## 验证证据

- 最终定向测试：5 个文件、23 项通过，包括 current.json、生成后的技能查询目录、三类来源筛选、反向查询、合法技能消费者、幂等和拒绝异常数据。
- Web/桌面全量：105 个测试文件，1811 项通过、1 项失败。唯一失败为新增麦芒没有可内置 PNG；未关闭该检查。
- 小程序全量：37 个文件、431 项通过；仅重建本地 bundled runtime，未打包小程序。
- `data:validate`：619 只精灵、580 个技能，重复身份及种族值错误均为 0。
- `data:verify-bindings`、完整 lint、`git diff --check` 通过；既有 SpiritStep CRLF 提示不是本次新增错误。
- 与 HEAD 快照逐实体比较：恰好 10 个精灵、10 个学习面、2 个既有技能发生变化，新增麦芒 1 个技能；所有特性未改。
- 最新 current.json 内容摘要：`41ae447a27dd7bba14c67afcc5cbeec2321c6482f4b97854b5731f517d786a01`。
- 浏览器实际点选本地 4176 预览：银月狼王 47/47；默认 14/47、血脉 18/47、技能石 15/47。4175 旧页面仍读到 6 项，独立端口用于排除旧页面缓存；未清理用户配置。此证据仅代表本地，不代表线上。

## 发布前未完成项

1. 麦芒缺少本地图标。当前图标完整性测试失败，不能称为全绿或可打包；没有拿其他技能图替代。
2. 网页产物编译成功，但性能门禁失败：当前客户端总量 13.03 MiB，大于硬上限 13.02 MiB；没有提高阈值。运行时数据 1.37 MiB 小于 1.50 MiB；CSS / JS 在警告区间但没有触发各自硬上限。当前包含其他工作区 UI 改动，本次未做无关资源裁剪。
3. 内置常用预设尚需单独确认：摇铃魔偶仍含旧午夜爆音 ID；智辉章脑仍含降雨、水波术。为保留用户准备调整的预设，本批没有直接重配该文件。活动学习面与当前默认槽已经按上述规则处理。

因此本轮完成的是正式数据纳入及本地验证，不是发布验收通过。
