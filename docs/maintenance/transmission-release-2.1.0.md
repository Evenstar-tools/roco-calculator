# 传动计算器 v2.1.0 同版本补发

日期：2026-09-17。用户明确要求传动工具随当前版本上线；本次追加传动功能与菜单入口，根版本保持2.1.0，不重打既有标签。此前9月16日文档记录各阶段本地状态与方案，不表示所有规划已实现；以当前代码及支持状态门禁为准。

## 发布范围

- 主站右上角菜单 → 查询工具 → 传动计算器。
- 四槽位置推演、精灵与特性联动、双方技能导入、逐回合推进与撤回、重置、初始预览和逐层来源。
- 不支持或未确认机制提前标记和禁用；机械变式、风速仪只支持顺序，不累计能耗和印记。本工具不是完整战斗模拟。
- 不带入机幕方舟第227条预设及其关联改动；不发布桌面安装包或小程序，现有电鹿页面继续保留。
- 修复开始按钮禁用后焦点移出弹层导致Esc失效；回归测试先复现失败，再通过文档级按键监听修复，嵌套选择器的事件拦截仍保留。

## 验收记录

- 最终153文件2329项单测通过；Esc修复后87项相关测试和完整单测均重跑通过。
- 120项Chromium端到端重跑全部通过。首次并行运行有1项原队伍截图超时，未改测试标准或业务逻辑，降低到2个worker后通过。
- 生产构建、16项验收矩阵、数据校验、共享内核current-only、小程序配置预检均通过；历史标签未改动。
- ESLint通过。与E2E同时运行时曾误扫test-results内生成的压缩资源，明确排除该生成目录后重跑，不放宽源码规则。
- 总资源15.74MiB、CSS gzip52.55KiB、JS gzip332.70KiB：软预算警告，硬门禁通过，未调整预算。
- 生产预览实际从菜单打开；声波缇塔的啮合传递／地刺／传感器／主轴四回合结果逐项匹配；撤回、初始预览、重置、盲拧阻断及切回恢复、关闭和Esc通过，无pageerror。
- 按design-craft检查1440、390、320像素截图，面板和预览无横向越界，浅色与深色保持主站控件风格。截图位于output/playwright/transmission-preview-*.png。

## 线上验收方法

推送origin/main后，等待自动部署；比对首页、/dianlu、Service Worker和全部构建JS/CSS等资源哈希，实际从线上菜单进入并重跑相同操作。发布成功以真实线上回读为准，不以本地构建代替。
