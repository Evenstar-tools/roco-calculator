# 传动四槽合并验收

按用户截图反馈，移除操作区开发说明长文；具体不支持的机制仍在触发时阻断并提示，不改变规则能力。

配置与结果共用一个四槽区域：初始可编辑，计算后显示当前顺序。初始配置按钮支持悬停、键盘聚焦和点击查看，预览固定按1、2、3、4逐行排列。邻近重置按钮恢复初始技能、清空回合记录，保留精灵、特性及布局。

验收：54项相关测试通过；新增计算后无重复编辑区、悬停展示及离开关闭断言。完整构建和性能门禁通过。Playwright在1440、390、320宽度确认预览逐行排列且不越出视口；重置恢复原技能，键盘聚焦可查看预览。桌面和320截图已读回。

后续界面调整检查：计算后只保留一组四槽；初始配置作为就地预览，不能再展开成第二组；开发机制清单留在维护文档，仅在实际操作需要时提供具体提示。

截图：output/playwright/transmission-layout/merged-1440.png、merged-320.png。仅本地修改，未发布。
