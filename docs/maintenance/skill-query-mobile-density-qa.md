# 手机技能检索密度修正验收

日期：2026-09-10。用户确认 `artifacts/web-ux6-mobile-density/comparison.png` 后实施，仅修改 Web / 桌面共用页的手机断点，不改小程序、计算数据或用户配置。

## 改动

- 手机已选技能改成可逐个删除的标签，数值保留在技能库卡片，不重复展示；长名称省略时保留完整标题和删除按钮的无障碍名称。
- 去除中间“查看匹配家族”按钮，只保留底部快捷导航。
- 效果开关移至搜索栏右侧，不再独占技能库标题行；随窗口跨过 650px 断点切换位置，始终只有一个开关。
- 保留常驻三个筛选、正文字号和 44px 删除点击区域。已选零项时不渲染可见的空操作区。

Design Craft 对照已确认预览核对结构与字号，不通过缩小文字解决拥挤。

## 新增密度门禁

测试文件：`tests/e2e/skill-query-cards.spec.js`。

除无横向溢出外，明确检查首张技能卡顶边、底部导航位置和完整卡片可见性。覆盖 320 / 390px、650 / 700px 高度、亮暗主题、0 / 1 / 4 个已选。

- 首卡顶边：0 / 1 项时不超过 350px；4 项时不超过 430px。
- 所有测试尺寸至少可见第一张完整技能卡，不被底部导航遮挡。
- 验证长名称移除按钮至少 44 × 44px、不出屏；删除后保留搜索。
- 验证手机 → 桌面切换只移动开关，不丢失查询状态。

## 证据与状态

参考截图：`artifacts/web-ux6-mobile-density/320-1-after.png`、`320-4-after.png`。

正式实测图：同目录 `actual-<宽>-<高>-<已选数>-<主题>.png`，共 24 张。已读回 320px 参考/实装同图对照 `reference-actual-320.png`、`reference-actual-320-four.png`，以及 320×650 四项暗色、390×700 一项亮色实测图；结构、字号、卡片位置与确认方案一致。

在 700px 高度下，一项已选的首卡从 531px 提前到 341px，释放 190px；四项已选从 738px 提前到 418px，释放 320px。650px 高度选满时，第一张卡片完整显示，不被底部导航覆盖。未缩小正文或删除按钮点击区。

首次全量 E2E 在构建阶段超过测试服务 60 秒启动限制，未产生浏览器断言结果。随后使用独立 `npm run build`，再以临时测试配置复用该构建启动同一 4174 预览；未放宽应用性能预算。

单测 113 个文件、1863 项通过；全量浏览器回归 82 项通过（2.2 分钟）；lint、生产构建及性能硬门禁通过。资源软预算仍有既有告警，未放宽门禁。首次截图等待误包含不可见懒加载图标，已修正为只等待有可见尺寸的图片，不改产品逻辑，随后全量回归通过。

本地 4175 预览已返回新构建的技能检索 CSS，确认包含手机紧凑规则。本批仅本地提交，不推送或发布；未重新打包桌面安装包或小程序。
