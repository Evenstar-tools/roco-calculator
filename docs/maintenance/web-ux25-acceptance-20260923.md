# Web UX25-01 实施验收

日期：2026-09-23
确认依据：用户回复“实施计划调整”，沿用 `web-ux25-plan-20260923.md` 中已出图的 UX25-01。
范围：Web 属性查询弹窗；未改小程序、计算规则或数据。

## 落地

- “对应属性精灵”从倍率表后移至已选属性与攻防倍率表之间；仅双属性显示。
- 去除结果区外层卡框及单卡边框，单项只保留头像和名称；不再显示容易与“最终形态”口径冲突的阶段字样。
- 760px 以下多结果在结果条内部横向滚动；结果列表可获得键盘焦点。
- 保留最终形态筛选、匹配数量、倍率来源点击和原有攻防表行为。

## 验证

- `npm test`：112 个文件、1917 项通过；新增断言覆盖位置、单属性隐藏、地＋冰切换地＋武后的 8 项结果及列表聚焦。
- `npm run lint` 通过；`npm run build` 通过，包含性能预算门禁；复用该构建运行 `E2E_PREBUILT=1 npm run e2e`，13 项通过。
- 实测 1920×1080、1280×760、390×844、320×844，亮暗两主题；地＋冰返回獠牙猪，地＋武返回 8 项。各视口页面 `scrollWidth` 等于视口宽度，弹窗正文无横向溢出，头像失败数 0。390/320px 下 8 项仅在内部列表滚动；320px 生产预览中聚焦列表按 `ArrowRight` 后 `scrollLeft` 从 0 到 40。
- 生产构建预览另拍 1280/390px 地＋冰及地＋武截图，结果位置与开发预览一致；[确认示意与生产构建截图合图](../../artifacts/web-ux25/acceptance/review/mockups-combined.png) 已读回。暗色和其余视口补充截图在 `artifacts/web-ux25/acceptance/`。

状态：本地已实施并验收；本文件不代表已推送或线上部署。截图产物留在项目 `artifacts/`，按仓库规则不纳入提交。
