# 竖屏标题栏修复

2026-09-18，纳入 v2.2.0 发布批次。后续修正：模式按钮从46像素高的上下排改为38像素高横排，文字简写为“精简／具体”；新增高度回归断言，重新验证以下全部视口。

仅在竖屏将精简版/具体版合为一个按钮，显示当前模式，点击切换到另一模式，继续使用已有模式记忆。旋转到横屏恢复双按钮；窄竖屏短标题显示完整“洛克计算器”，宽屏赛季标题和工具页面标题不变。

按 design-craft 保留已有图标、配色、按钮高度，不堆叠第二层导航。实测 320×844、390×844、768×1024、844×390、1280×900 的亮暗主题，共 10 个状态，标题无裁切、栏内无溢出；同时验证双向切换、旋转、刷新记忆。截图 `output/playwright/header-portrait-<宽度>-<light或dark>.png`，320 暗色、390 亮色已读回。

新增竖屏组件回归及工具箱测试共 3 项通过；前端构建、硬体积门禁通过；17 项手机布局与新手引导浏览器回归全部通过。没有重复运行本轮之前已通过的全量回归。

后续标题栏改动验收清单必须检查“标题实际 scrollWidth 不超过 clientWidth”，不能只检查整栏是否溢出；竖屏模式按钮数量为 1，横屏为 2，并验证旋转不改变当前模式。相关约束已加入组件测试与本地浏览器验证。
