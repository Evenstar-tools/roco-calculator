# 第四轮小程序界面调整验收

日期：2026-09-09。仅本地实现，无上传或安装包。

## 确认范围

- M1：参数页标题显示对应阵营与精灵名，60 个体用阵营淡色底区分，数值逻辑不变。
- M3：按最新确认将「重置本页」置于设置列表第一项；保留既有确认与收藏保护。此确认替代原先下移方案。
- M2 不实施；结果栏和字号保持原样，不能以缩小文字换紧凑布局。

## 验证与证据

- 小程序全量测试：37 个文件、431 项通过。
- `npm run miniapp:build` 通过（development 模式 weapp 构建）。
- 独立 H5 浏览器验证 390、320、768 宽度，亮暗主题，双方参数标题、60→0→60 底色切换、重置第一项、页面无横向溢出。
- 实测图与指标：`artifacts/mini-ux4/implemented/`；M1 同状态参考对比：`m1-fidelity.png`。参数标题改为一行紧凑排列，底色沿用现有阵营主题变量，弱化边框，尺寸字号不缩小。
- M3 实测：`390-light-settings.png`，以最新用户指令为准，未使用原下移示意图作为验收目标。

## 原生验收进展

微信开发者工具 CLI auto 返回成功，但 9421 实际是 IDE HTTP 端口，WebSocket 返回 404；按现有 automator 的 auto-port 9420 启动后仍拒绝连接。未变更安全设置，也未清理用户数据。

以上为此前连接失败的记录，现已恢复，见下方补验。仍需完成 iPad 与原生暗色覆盖，不能宣称发布就绪。

2026-09-09 14:45 补查：9420、9421 均无监听；进程 101152（`wechat_devtools_2.02.2609082_win32_x64.exe`）自 14:42:07 启动，等待 20 秒后仍未退出。当前可确认的是新版安装程序仍运行，不能据此断言安装完成或失败。先等待安装结束，再核对实际 CLI 版本与自动化端口；不在安装切换期间重复启动旧工具、修改工具文件或更改安全设置。

2026-09-09 15:05 补验：安装进程已退出，CLI 明确返回 `autoPort: 9420`；9420 WebSocket、9421 IDE HTTP 均实际监听。开发者工具 2.02.2609082、基础库 3.16.2 连接与启动成功。

- `npm --prefix miniapp run build:weapp -- --mode production` 通过，未上传或制作安装包。构建后 `miniapp/dist/pages/index/index.js` SHA-256：`7AD52A9C813BA2CBAE56C4996E8BECEBB2B35673B74779CDCFC15D222A63F8EC`。
- iPhone 12/13 (Pro) 原生模拟器，390×844 屏幕、390×753 内容窗口，亮色。M1 双方标题、个体 60→0→60 样式切换；M3 重置为正文第一项、原生确认框、取消保留设置弹层，均通过。
- 攻击方满个体背景 `rgb(255,245,239)`，防守方 `rgb(239,247,252)`；标题实际高 24px，文字一行。没有缩小字号。测试后恢复输入原值，重置只取消，未执行清空。
- `artifacts/mini-ux4/native-implemented/` 包含双方参数页、设置、原生重置确认截图及 `report.json`，四张截图已读回检查。运行期异常列表为空。截图包含模拟器原生状态栏和安全区，不与 390×900 H5 整屏作逐像素等同比较。
- 待补：iPad 原生布局与原生暗色状态；已通过的 H5 六套截图不能替代这些覆盖。
- 只读核查当前工具的 `MiniProgram.d.ts`、`Native.d.ts`、`Launcher.d.ts` 及自带 `wechatide-skill` 工具目录，未找到受支持的切换设备或系统主题 API。内部 UI 动作不作为公开自动化入口使用。后续请在开发者工具模拟器手动选择 iPad 与暗色，再复用当前 9420 连接补验；不修改工具内部实现或安全设置。

## 回退与后续门禁

本批独立提交仅包含 M1、M3 组件、样式和测试，可单独 revert，不混入其他在途功能。后续调整必须断言设置正文第一项为「重置本页」，避免再次按一般低频危险操作假设下移；最终顺序以本项目玩家操作频率为准。
