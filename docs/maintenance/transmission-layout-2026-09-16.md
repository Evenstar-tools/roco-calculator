# 传动计算器布局与技能图标验收

日期：2026-09-16。用户要求竖排与2×2（12／34）切换，并加入技能图标。本轮完成此项功能，不代表此前计划中的全部机制已经实现。

## 改动

- 初始配置与当前技能槽位共用布局切换，默认2×2，竖排依次显示1、2、3、4号位。
- 切换布局只改变展示，不重置技能、回合或历史；重置推演保留本次打开期间的布局选择。
- 当前槽位复用SkillIcon及项目现有技能图标、失败回退；初始选择器沿用已有图标。图标随技能传动，不绑定槽位。
- 修正320像素下关闭按钮换行。

## 验证

- 传动引擎与界面测试52项通过；组件ESLint通过。
- 完整构建、验收矩阵、计算核心同步、资源绑定与性能门禁通过。
- Playwright真实浏览器分别检查320、390、1440像素宽度的两种布局，初始配置及当前四槽顺序正确，无横向溢出。
- 四张技能图标成功解码；布局切换保留技能顺序；切换后完成行动并成功开始第2回合。
- 已读取桌面及手机截图进行视觉检查。截图目录：output/playwright/transmission-layout/，文件名为宽度-grid.png或宽度-vertical.png。

## 边界

仅修改TransmissionPanel.jsx和transmission.css，并新增本记录；保留其他任务改动。未提交、推送、打包或发布。轮班未知结算、全队和身份变化等原有规则边界仍保留，见此前调研计划。
