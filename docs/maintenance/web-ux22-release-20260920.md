# Web 2.2.2 同版本提交记录

范围：布灵队伍系别统计、阵容导出模式及同名技能 ID 修复、长期体积预算。用户已要求同版本提交并回查 Web；不重打桌面包、不发布小程序，不纳入其他未跟踪工作文件。

## 本地证据

- 最终源码的常规测试 107 文件 1833 项通过，lint、输出编码检查通过。
- 完整构建通过：JS gzip 348.49 KiB，CSS gzip 56.04 KiB，低于 400／72 KiB 预警线；版本仍为 2.2.2。
- 前轮已完成 1920/390/320px 深浅色布灵统计检查，以及真实剪贴板导出、重新导入验证；对应脚本与图片见 `artifacts/web-ux22/`、`artifacts/lineup-export-fix/`。
- 最终 E2E 首轮竖屏模式按钮用例曾在缩窄后仍读到两个按钮；单项及完整 13 项复测均通过。该组件未在本次修改，未放宽断言。此偶发现象作为后续复核项，不冒充已修复。

## 发布回查方式

- 逐文件暂存本次改动，推送唯一公开仓库 `Evenstar-tools/roco-calculator` 的 main。
- 核对远端提交及该提交的 CI，不以本地提交成功替代发布成功。
- 用 `artifacts/web-ux22/verify-live-release.mjs` 比对首页、全部 JS/CSS、阵容映射与数据文件的 SHA-256。
- 在独立线上测试浏览器验证布灵统计、动态更新、PVP 默认选项和带复写技能的真实复制／重新导入；完成后恢复测试会话存档，不修改用户在用的浏览器存档。
- 最终线上哈希、截图、CI 结果写入 `artifacts/web-ux22/`；本记录不将待执行检查写成已通过。
