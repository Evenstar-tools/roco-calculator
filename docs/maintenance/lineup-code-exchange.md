# 游戏／千岛阵容码交换

2026-09-10：Web 与 Windows 共用队伍面板新增“导入阵容”“导出阵容”。

## 用户流程

- 图片导入（2026-09-12）：队伍 → 导入 → 上传配队图。支持含二维码的 PNG／JPG／WebP，最大 10 MB、2400 万像素。浏览器／Windows 本机使用随包附带的 ZXing-C++ WASM 识别，自动进入同一个阵容预览，确认后另存新队伍；不上传图片、不跟随二维码链接、不做 OCR。可展开查看二维码原始内容。
- 导入：粘贴裸码、带 `shareData` 的分享链接，或游戏内复制的整段分享文案。文案只提取独占一行的阵容码，支持 `#` 标题前缀与 `\~`、`\_` 转义；精灵、血脉、魔法等字段全部以原码为准，多套不同阵容码明确拒绝。解析后收起长码，头像预览采用桌面两列、手机单列；“修改代码”可重新编辑并清除旧预览。确认个体处理方式后，“保存并调整个体”另存为新队伍，选中首个非空成员并聚焦个体输入。原队伍不覆盖；一次写入整个队伍。
- 未恢复或未采用推荐的导入成员显示“个体待设置”，修改实际个体值后清除该成员的提示；仅修改性格、技能或血脉不会清除。提示状态随队伍保存、刷新、复制保留，不写入游戏阵容码。
- 导出：选择共鸣魔法、编队模式，复制阵容码或官方分享链接；点击复制时保存分享设置。界面只保留一个码区，剪贴板失败时切换为对应的完整代码或链接并选中，供手动复制。
- 实际个体数值、月相记忆及战斗条件不属于游戏阵容码。个体未编辑时原样保留来源字段；编辑后导出最多三项已选属性，不编码具体数值，超过三项明确拒绝。
- 目前支持含性格改写尾部的 v1 格式（`8 + 39 × N` 字符，1～6 位）。未知版本、未知映射、无法表达的性格改写及超过四个非空技能会明确报错。

## 结构

### 个体配置判定与推荐

- 官方属性列表 `https://rocom.qq.com/cp/rocom_game_manager_json/prod/sprite/individual/list.json` 于 2026-09-10 核对：1 生命、2 物攻、3 魔攻、4 物防、5 魔防、6 速度。三项均为空才是“个体未配置”；有效不重复选择按计算器 60 档恢复，预览明确显示“各 60，非原始数值”。
- 不在列表中的值或重复值标为“原码个体待确认”，不能误判为空；真实千岛夹具的三项均为 80，仍保留原码，不提供自动推荐。
- 仅缺失成员提供默认不选的“使用推荐”复选项及“缺失项全部推荐”；取消勾选即撤销。优先匹配内置同精灵、同性格预设，以四技能重叠数排序；无匹配时按性格与四技能方向提供三项 60 的参考，避开性格降低项。预设按需加载，失败则使用明确标注的参考规则。
- 推荐只写入这次导入的新队伍，不覆盖已有队伍或手动值。原码有效选择、未知字段均不受批量推荐影响。带 shareData 的完整分享链接与裸码行为相同，不以链接长度判断配置缺失；不支持不透明短链解析。
- `tests/state/lineup-ivs.test.js` 固化缺失／有效／未知三类、手动值保护、推荐与导出回归，避免再次把面板零值误写为原码无配置。

- `src/state/lineup-code.js`：纯解析／编码函数及计算器配置适配。无网络请求、无存储副作用。
- `public/data/lineup-code-map.json`：随构建携带的 ID 对照表，打开交换面板后读取本地文件，不依赖千岛／腾讯在线接口。来源与核对日期在文件内。
- `src/components/TeamExchange.jsx`：加载、预览、保存、导出与复制。
- `src/state/lineup-image.js`：本地图片二维码识别，识别器按需加载；只允许官方完整阵容链接或阵容码候选进入协议解析。普通链接／短码显示原始内容并拒绝导入；未知 ID／未知协议继续由原解析器拒绝。无码、模糊码、多种二维码及非法图片均有明确错误。输入文字会取消旧图片结果，防止异步覆盖。
- `team-presets.js`：队伍级 `lineup` 保存魔法、模式及原位置数量；成员级 `lineupSource` 保留游戏原始字段，支持编辑后按字段重新编码；`ivsPending` 记录新导入成员的个体待设置状态。原版本队伍无需迁移。

映射生成方法：原有游戏精灵 ID 通过 BWIKI `Module:LineupData/Pets` 的精确 `title` 对应本地唯一 `fullName`，技能通过官方列表精确名称对应本地唯一技能名。S4 补全使用 `data/reviewed/nrc-2026-09-10.json` 的 `game_id` 和本地实体 `provenance.nrc.id` 精确连接，并与官方列表交叉核对。当前包含 622 个精灵 ID、590 个技能 ID；覆盖全部 622 个本地精灵、579 个正式技能，另有两个旧技能名的显式导出别名。不得改成模糊匹配；存在多个反向 ID 时，优先使用该成员导入时的原 ID，新建成员无法消歧则拒绝导出。

维护映射时先核对实际形态，尤其是同名多形态和首领。加油蟹 3743 必须对应“单只海葵的样子”。外部资料只用于事实 ID 对照；解析器是本项目独立实现，没有执行或复制外部网页脚本。

### 2026-09-12 全量 ID 补全

- 在上一轮银月狼王、布灵布灵、月蚀修复基础上，再补 26 个精灵形态、25 个正式技能 ID；全部本地实体的映射缺口归零。
- 满月砣 `5064` 为上弦、`5065` 为下弦，已用官方详情的物攻／魔攻与本地形态种族值交叉核对。幽影树突变形态 `3777` 来自已审核的 Catalog revision 7265；本次官方列表未列出该项，不能把它混同为普通幽影树 `3064`。
- `skillAliases` 仅处理已保留用于旧配置兼容的“降雨 → 落雨”“午夜爆音 → 午夜噪音”：导出采用正式游戏 ID，重新导入恢复正式名称，不编造新 ID，不改动旧配置存储。
- 官方列表包含项目未建模的精灵、随机占位、测试项，以及特性、应对效果等。它们不是本地实体的映射缺口，不因名称相近而导入：例如寒音蛇 `3622` 的形态和图鉴号不同，`19000008/19000009` 与本地同名精灵种族值不同。
- `tests/state/lineup-mapping-coverage.test.js` 校验全部本地实体覆盖、审核资料一致性、全部 1212 个外部 ID 的导入导出往返、无来源字段的新建精灵导出、旧技能名兼容和形态区分。加上真实阵容与交换 UI，37 项相关测试通过。

## 验证

- 真实夹具 `tests/fixtures/qiandao-lineup.json`：用户提供的 S4电鹿轮转，242 字符、6 人、24 技能、进化之力。
- 状态测试覆盖真实样本、URL／转义、错误输入、空技能及单成员、性格改写、编辑后导出、四技能上限、新队伍个体选择留空、整队原子保存、刷新／复制保持来源及存储失败。
- UI 测试覆盖个体数值确认、输入变化清除旧预览、剪贴板失败手动复制。
- 浏览器实际执行导入、命名、保存、刷新后导出、两种复制；阵容码剪贴板内容与原码一致。390 像素宽度检查队伍面板无横向溢出，窄屏工具栏使用图标。
- 导出链接已在官方分享页实际显示正确队名、六人及全部 24 技能；官方页的编队标题仍是页面固定文案，不作为模式验证依据。
- 未在游戏客户端或千岛 App 中执行回导；小程序现有队伍分析与此 Web／桌面队伍仓库不同，本次未改小程序。

参考：[官方分享页源码](https://rocom.qq.com/act/a20250703array/js/index.js)、[BWIKI 字段结构](https://wiki.biligame.com/rocom/Module:LineupCode)。

### 2026-09-12 图片导入本地验收

- 真实夹具：`tests/fixtures/lineup-images/real-qiandao-lineup.jpg`，解码为腾讯官方完整阵容链接，队名“雨中小故事”；依协议顺序为爵士鹿、幻影灵菇、飞飞钥、小皮球、荆棘电环、寂灭骨龙，共 24 技能。保存后再次导出的阵容码与二维码中的原码完全一致。
- 浏览器回归 `tests/e2e/lineup-image-import.spec.js`：真实图片、无码、重度模糊、未知短码、普通 URL、多码、损坏协议共 7 项通过。测试阻断并记录外网请求，结果为零；使用桌面端相同 CSP。390px 下无横向溢出。
- Windows Electron 实际加载 `app://calculator/`，独立临时用户目录、隐藏窗口运行，同图成功解析，外网请求及页面异常均为零。桌面 CSP 仅新增 `wasm-unsafe-eval`，未放开脚本 eval；WASM 使用正确 MIME。
- 证据：`output/playwright/lineup-image-import/desktop-upload-entry.png`、`desktop-preview.png`、`mobile-preview.png`、`electron-preview.png`、`electron-result.json`。原始载荷保存在 JSON 证据中。
- 生产编译成功，**性能体积门禁未通过**：客户端 14.52 MiB > 14 MiB、JS 原始 922.50 KiB > 880 KiB、JS gzip 273.97 KiB > 272 KiB。新增识别 WASM 953.52 KB、按需 JS 35.16 KB；共享工作区还包含其他未提交功能。未修改性能阈值，未打安装包、部署或推送。此结果不能记作完整发布验收通过。
- 当前接入范围为 Web／Windows 共用队伍面板；小程序未接入。Web 首次识别需能加载本站随包资源，缓存后可复用；Windows 使用本地安装资源，无需 Python 服务。

### 2026-09-10 提交验收

- 功能提交 `60c907c`：共享工作区全量单元测试 1852 项、lint 通过；独立干净检出专项及 App 集成测试 131 项、生产构建及浏览器回归 71 项通过。共享工作区首次浏览器回归的两项入口等待超时未在独立检出复现，没有改动无关测试。
- 独立测试浏览器完成 1440px 桌面及 390/320px 手机深浅色检查；推荐默认关闭、批量采用与单项撤销、未知字段保护和保存后数值均通过。头像 44px、手机内边距 14px、推荐行点击高度至少 44px，未添加新弹窗。
- 相较先前预览，推荐总操作行和缺失成员的推荐行会增加高度；这是展示可选配置所需的明确差异，手机保持纵向滚动，不缩小原技能文字。实测截图存于 `output/playwright/lineup-ivs-*`，仅为本地 QA，不计入发布资源。
- 未打桌面安装包、未做游戏客户端回导；代码推送与线上部署分别验收。
