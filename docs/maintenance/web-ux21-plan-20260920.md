# Web UX21：常用配置可理解性与查找

## 范围与边界

本轮按照 ui-ux-preview 的“复核待办→临时模拟→确认后实施”流程；design-craft 用于约束原字号、原色彩、紧凑布局，Playwright 用于真实页面复现与亮暗截图。现有候选已处理，本次仅补查常用配置弹窗，不做全站重新扫描。

- 基线：`53e4556` 加当前工作树，Web 2.2.2，复用本地 4191 服务；未重启服务。
- 独立测试会话：`ux18-local`，1920×1080，具体版，亮暗主题。
- 数据：内置 230 项预设；测试浏览器旧配置显示相同 217、不同 10、新增 3。不同项中 9 项将更新、1 项保留手改；按钮更新 12，计算正确。
- 用户此前“先别提交”继续有效：没有提交、暂存、推送、发布或导入配置。产品源码没有本轮新增修改。

## 待确认项

1. W21-01：开关从“仅预览配置项”改为“仅看预设与收藏”。现有逻辑已包含手动收藏，只把可见文字说明准确，不改变浏览范围或导入范围。
2. W21-02：常用配置清单搜索支持已有精灵别名。实际输入“圣甲虫”没有结果，正式名“圣凯布米龙”有 1 项。模拟复用正式名结果节点展示期望命中，不将模拟当作搜索功能已完成。
3. W21-03：将“不同”拆为“待更新、保留手改”；相同、新增继续保留。使待更新 9＋新增 3 与按钮 12 对得上，不修改保护手改的导入逻辑。

另记录两项排除：清单已经有清空搜索入口；更新数量没有算法错误。不强行增加新功能。

## 证据

目录：`artifacts/web-ux21/`。

- `capture-proposals.js`：复现、真实截图、逐项临时 DOM/CSS 模拟和还原检查。
- `w1/w2/w3-before/after-light/dark.png`：每项同视口、同主题、同数据、同截图尺寸。
- `comparison.json`：三个候选合图输入。
- `proposal/mockups-combined.png`、`proposal/preview.html`、`proposal/evidence.json`：效果模拟交付。

脚本返回两主题 updating=9、retained=1；恢复后的名称“仅预览配置项”，汇总“相同 217／不同 10／新增 3”，无 data-ux21 残留。未替换 React 区域 innerHTML，保留原节点并撤销临时克隆。合图与亮暗关键截图已实际读回；发现首轮模拟计数重复后修正脚本并重拍，最终显示 1/230。

## 确认后的验证要求

- W21-01：既有预设、手动收藏、非收藏、输入搜索的范围不变。
- W21-02：别名只匹配已收录预设；保留正式名搜索、大小写及空白处理、清空焦点，不擅自扩展模糊搜索算法。
- W21-03：相同／待更新／手改／新增归类数与按钮一致，覆盖全相同、全手改、仅新增、混合情形；不改变预设历史识别或导入规则。
- 1920×1080 亮暗实际截图＋320/390px 受影响区域兼容检查；本轮尚未进行真实实施或窄屏验收。
- 相关测试和项目实施门禁，确认构建次数避免重复；维持 JS 348.25 KiB / CSS 56 KiB 已授权上限，体积不够时另行讨论，不自行加预算。

当前阶段：效果模拟，尚未实施，等待编号确认。
