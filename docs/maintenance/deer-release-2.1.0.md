# 电鹿斩杀线 2.1.0 发布验收

日期：2026-09-16。范围为电鹿独立页面与必要主页入口，不包含工作区另行开发的传动计算器、机幕方舟预设或小程序独立更新。

## 本地发布候选

- 隔离工作树：D:/codex/worktrees/roco-deer-210，起点 origin/main 9b33919。
- 根版本、锁文件、用户版本记录、仓库日志和 Service Worker 缓存统一为2.1.0。
- 151个测试文件、2267项测试通过；全仓ESLint、数据校验、16项验收矩阵、当前共享内核一致性通过。
- 生产构建及120项Chromium端到端测试全部通过。
- 资源量15.72MiB、CSS gzip 50.98KiB、JS gzip 325.77KiB；均通过硬门禁，仍有软预算警告，没有调整预算。
- 生产预览直接访问与刷新 /dianlu、通电两层引电复算、11档逐层查询、一键填层数、返回主站均通过，无页面运行错误。
- 1112px和320px确认返回按钮位于主题按钮之后的最右侧，标题未截断、无横向溢出，深浅色截图读回通过。按design-craft保留深蓝顶栏并以淡金描边呼应标题。
- 当前共享内核检查通过；完整历史标签检查仍报告旧提交 ec57cbcb 的两处镜像差异，本次未修改历史标签或共享内核。
- 隔离环境首次复用依赖目录导致Vite外部资源路径被拒绝；已独立执行npm ci，以上测试全部来自独立依赖重跑，不采用此前中断结果。

## 发布后验收

推送后核对 origin/main、线上根路径和 /dianlu、Service Worker版本、发布JS/CSS及运行数据哈希，并在真实线上页面复测交互。线上结果待部署完成后补记。

此轮网页发布不包含新桌面安装包或小程序上传；不把网页可用表述成其他平台已交付。
