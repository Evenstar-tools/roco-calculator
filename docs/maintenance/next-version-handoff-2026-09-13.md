# 后续版本开发交接 · 2026-09-13

## 接续目标与边界

用户要求将「洛克计算器｜新赛季更新准备」交接到新任务，并归档原任务。仓库为 `D:/codex/rock-calculator-public`，新任务继续承接后续版本需求，不重做已完成的 S4 更新。

首次接手只读核对本交接、项目规则与当前工作区，简短确认接收后等待用户的新版本具体需求。此交接不授权自行升级版本、提交、推送、打包、上传、飞书通知或扩展功能。

## 当前真实状态

- 源码版本：Web／桌面 `2.0.2`，小程序 `1.1.16`。以两端 package.json 为准，不把版本号当作已发布证明。
- 公开仓库 `https://github.com/Evenstar-tools/roco-calculator`；网页 `https://rococalc.top/`。本次交接没有检查线上更新或安装包最新状态。
- 交接核对时位于 main，HEAD 为 `24b40e5`，本地跟踪显示领先 origin/main 6 个提交；此值没有 fetch，且另一任务正在改变 Git 状态，不能作为后续发布依据。
- 「洛克计算器桌面版｜基础开发」（任务 ID `01a03327-75ff-73b0-9f13-56b36cc6a890`）正在同一仓库处理提交／发布。大量 Web 改动已暂存，design-qa.md 尚有额外未暂存收尾记录；draft_189d29c5_folder、prototypes 等非本次交接产物保留。不要 reset、清理、撤销暂存、全量 add 或重复发布。
- 本交接只新增文档，不代替该发布任务收口。后续动手前重新核对 git status、日志、跟踪分支与相关任务状态。

## 已完成能力与资料入口

- S4 正式数据、赛季主题和深浅色适配已进入 2.0 系列；当前不应再沿用 S3 季中／S4 前瞻作为产品标题。不要把 9 月 10 日前的占位计划当作现状。
- 计算器具有单／四技能与双向比较、已适配特性条件、雷暴和异常结算、特殊技能联动、速度／耐久排行、队伍／预设／阵容码等能力。准确范围以代码及 `CHANGELOG.md` 为准，不宣传未经验证的“全机制／全部特性”。
- 2.0.2 更新记录另包含配队图／分享文案导入、速度查询与缓存改进。这些有其他任务参与，接手不得假定全部是原任务开发或已公开交付。
- 阅读顺序：`AGENTS.md` → `docs/maintenance/maintainer-handoff.md` → `CHANGELOG.md` 当前版本 → `docs/maintenance/skill-damage-comparison.md` → 本文验收记录。旧阈值方案见同目录 `skill-damage-comparison-threshold-plan-2026-09-13.md`，只采用首节最小收敛版，后面的大方案已搁置。

## 本轮核心交付：承伤对比与结论导出

- 入口在显示设置，承伤对比默认关闭；启用后从技能结果进入。固定一招和条件，比较全精灵满血承伤，按当前筛选／排序查看和代入复算，可撤回。
- 榜单不计候选防守特性，不含通用两回合模拟或回合末异常结算。代入后恢复目标特性，结果可能改变；不能把榜单当作全部实战保证。
- 耐久模板包含统一性格模板、当前防守方配点、用户预设。用户预设只取性格和个体，不复制技能／特性条件；存在可用本地配置时展示，可用配置严格大于 200 条时优先默认，用户手动选择优先，缺省候选有回退说明。
- 用户撤回了“未击倒改为大于50%但未击倒”的要求，保留原筛选与文案。
- 离心舞者等手动特性条件已修正为当前显式值优先，不被旧预设覆盖。榜内可显式沿用星陨／冻结；交接时 useDamageComparison.js 的默认值仍是 `previous.inheritTargetStatuses ?? false`，并不是检测到星陨就自动勾选。用户曾要求检查自动勾选，后续若问起须如实说明，不把历史请求描述成已实现。
- Web／PC 独有导出入口：承伤榜右上角「导出」→ Excel／Markdown。当前筛选排序的完整名单全部导出，不限界面首批 60 行；条件摘要＋九列表格，文件名含技能、状态层数、模板、筛选和时间。小程序不加导出。
- XLSX 是真实单工作表，固定表头、筛选、数值 HP／百分比；文本不执行公式。Markdown 转义特殊字符。全程本地生成，不上传配置。
- 导出版本标注已修复：当前计算快照的数据／规则版本优先，不能被导入旧存档的版本污染。正反方向均有回归覆盖。

### 关键文件

- `src/components/DamageComparisonDialog.jsx`：Web 入口、列表及导出交互。
- `src/features/damage-comparison/`：选择状态、异步计算、`export.js`／`export-xlsx.js`。
- `src/domain/skill-damage-ranking.js`、`src/state/damage-comparison.js`：批量计算及代入。
- `src/styles/24-damage-comparison.css`；`tests/ui/damage-comparison-export.test.jsx` 及既有榜单测试。
- XLSX 仅新增轻量 fflate 依赖，按需加载；不要重加已移除的大型 Excel 库。

## 最新验收与证据

- 导出及相关定向测试 46 项通过；全量并行报告 1975 项通过、3 项失败，失败所在文件低并发复跑 31 项全部通过。不是一次性全量绿灯，不应改写记录。
- Excel 普通模式只读打开，174 条名称及伤害与参考榜逐项一致；数值比例、筛选、冻结表头正常。
- 独立生产构建 `dist/export-closeout-client` 成功。实际 Electron 入口、隔离用户目录、`app://calculator/` 离线资源，两种格式下载均 completed。未修改用户安装或原配置，不等同于新 EXE 安装验收。
- ESLint、共享核心当前一致性、git diff --check、交付文字 UTF-8 门禁通过。
- 当前工作区门禁下，总量 14.58 MiB、JS gzip 293.17 KiB、原始 JS 972.45 KiB、CSS gzip 45.59 KiB，硬门禁通过，仍有警告。本轮未改门禁；预算由另一任务更新，旧记录中的失败不是当前独立构建结论。
- 证据目录 `output/playwright/`：`damage-export-closeout.md`、`damage-export-closeout-tests.json`、`damage-export-closeout-recheck.json`、`damage-export-example.xlsx`、`damage-export-electron.md`／`.xlsx`／`.png`、`damage-export-desktop.png`、`damage-export-320.png`。这些是本地过程证据，可能被 Git 忽略，不能假定新 worktree 自带。
- Dancer 历史参考 `output/dancer-starfall-20260913/recheck-results.json`：用户预设下 1 层特性、翼击、星陨 6／8 层为 83／174 个可击倒候选；统一生命模板为 56／107。仅是当时数据与配置的验算记录，条件变化必须重算。

## 后续执行约束

1. 优先复用旧测试文件；保持界面简洁，截图级问题须在指定窄屏实测，不顺便扩展求解器或模板管理。
2. 核心权威在 src/domain；小程序共享目录由同步脚本生成，不能单独手改。首次只读接手不运行 npm ci、同步或全量构建，避免干扰正在发布的任务。
3. 真正计算 PVP 伤害时先读取 `.agents/skills/rock-calculator-cli/SKILL.md`，使用项目 JSON CLI；不靠截图估数，不将整份数据加载进模型当计算器。
4. 测试和会重写运行时资料的构建必须串行；并行曾造成 JSON 半写读入和超时。先确认没有其他任务构建，再跑完整回归，避免反复重跑与资源争抢。
5. 存量用户配置、收藏、队伍和形态 ID 保持兼容；不能把预览预设擅自覆盖用户配置。
6. 发布、安装包、上传、平台审核、线上验证与飞书送达各自验收；当前交接不授予这些操作。后续有明确要求再按 AGENTS 和发布清单执行。

原任务 ID：`01a06037-84fc-7251-ae6c-57d4516c1334`。新任务是接续入口，原任务归档后仍可查阅历史。

## 接收确认

新任务「洛克计算器｜后续版本开发」：`01a099fe-35f1-7ab0-aaf1-0215a2211793`。已完成只读接收核验并确认导出代码、版本修复与 46／31 项验收证据。接收时 HEAD 已变为 c35bf0c，进一步证明上文 Git 数字仅为交接编写时快照。新任务未改文件或执行发布，等待用户后续具体需求。
