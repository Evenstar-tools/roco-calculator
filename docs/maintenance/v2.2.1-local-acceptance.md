# v2.2.1 本地提交验收

日期：2026-09-18。分支：`fix/analysis-phase1-20260918`。版本：2.2.1。

## 本次用户问题

- 原生技能列表滚动条：按住时焦点跳到技能行，触发失焦关闭。列表现在可承接原生焦点，不阻止滚动条 mousedown；选中或 Escape 后焦点回到输入框。真实鼠标按住650毫秒、下拖、回拖、选中、滚轮和外部点击均有回归。
- 当前配置摘要：速度与综合耐久前置，双防耐久次级排列；保留完整数值，明确是当前配置。移除旧的重复分隔规则，滚动定位避开固定导航。
- 提交同时收口此前确认的队伍/个体格/速度控件防挤压、传动窄屏与深色修复、推荐速度约束与重复计算修复。计算结果及保存 schema 不因视觉布局变动而改变。

## 本地验收

| 检查 | 结果 |
|---|---|
| Web单元/集成（去重合并） | 2614项通过，172文件 |
| 最后一轮相关单测补跑 | 49项通过 |
| 导出/配置库回归补跑 | 16项通过 |
| Chromium全量 | 182项通过，0失败，0跳过 |
| 小程序测试 | 491项通过 |
| ESLint、共享核心、16项验收矩阵、数据绑定 | 通过 |
| 生产构建、原有硬体积门禁、git diff --check | 通过 |

最初完整Web单测有2612个断言通过，但构建配置测试在jsdom中导入URL失败，未收集到其两个断言，因此该轮退出码为1。改为独立纯分包模块/Node环境后，这两个测试及受影响UI、日志测试补跑通过；上表为按文件覆盖旧结果后的去重统计，不冒充第一次完整运行全绿。小程序首轮另发现版本元数据与压缩镜像不同步，已用正式生成脚本同步并全量复测通过。

## 体积与版本

- JS gzip：350597字节（342.38 KiB），硬上限344 KiB。
- CSS gzip：55872字节（54.56 KiB），硬上限55 KiB。
- 清理源代码已无引用的旧榜单样式，版本消费者仅导入package.json的version字段，避免把完整脚本/依赖元数据打入前端；不提高预算。软预算警告仍保留。
- 根版本、lock、Web缓存与预设元数据统一为2.2.1；常用预设JSON和压缩镜像同步；小程序独立版本不变。

## 真实截图与证据

- `artifacts/v2.2.1-ui/review.html`：本轮前后摘要和拖动后真实网页截图。
- `artifacts/v2.2.1-ui/summary-card-390-light-after.png`、`summary-card-390-dark-after.png`：实际构建截图，不是AI概念图。
- `artifacts/v2.2.1-ui/native-drag-light.json`、`native-drag-dark.json`：真实鼠标拖拽位移与选中结果。
- `artifacts/v2.2.1-ui/final-gates.json`、`final-browser.json`、`first-full-unit.json`、`unit-recheck.json`、`exports-recheck.json`：退出码及原始验收结果。

最终构建和浏览器验收冻结本地产物，使用独立临时端口；最终验证期间源码指纹前后相同：`6af51f6cd757c8238bea57cf71d083efa7a1baa609fa134775614006c0a16193`。

边界：本地代码提交，不代表线上发布。未推送、未打标签、未制作或安装Windows安装包；iPhone键盘按视觉视口模拟验证，不冒充Safari真机验收。历史验收草稿及临时文件不混入本次提交。
