# 界面模式记忆

2026-09-18，本地开发，未提交或发布。

精简版和具体版切换后写入独立的显示设置，刷新或重新打开时恢复上次模式；首次使用及无效记录默认精简版。从电鹿返回时优先恢复已有工作区模式，加载中的标题栏也显示相同模式。存储读取或写入失败不会阻止当前会话操作，不修改精灵配置及撤回记录。

浏览器实测两种模式切换后刷新均保留；新增设置测试覆盖默认值、非法记录、双向读写及存储异常，集成测试覆盖卸载重开恢复。仅当前浏览器或桌面应用的存储范围内有效，不跨设备同步，清除站点数据会清除记忆。
