# Web 小优化待办池

## UX25 属性查询结果可见性 · 2026-09-23

基线 `43db610`，线上 Web 亮色主题，地＋冰；相关逻辑和图片已回查，方案尚未实施。

| 编号 | 优先级 / 状态 | 问题及证据 | 建议 / 验收 |
| --- | --- | --- | --- |
| UX25-01 | P2 / 已验收（本批提交） | 1280×760 首屏仅见精灵区上缘，390×844 首屏完全看不到；`artifacts/web-ux25/proposal/type-query-before-1280.png` 与 `type-query-before-390.png`。 | 用户确认后已将只读最终形态结果移到已选属性下方、倍率两栏前方，取消外层卡框与重复阶段字样；多结果窄屏在本区内滚动。详见 `web-ux25-acceptance-20260923.md`。 |

原方案阶段专项 225 项测试通过，622 个精灵头像文件齐全；临时页面模拟已恢复。实施阶段 1917 项单测、Lint、生产构建与 13 项 E2E 通过，实测截图已读回。候选详情与验收分别见 `web-ux25-plan-20260923.md`、`web-ux25-acceptance-20260923.md`。

## UX24 属性查询与电鹿入口 · 2026-09-22

基线 `60a36f9`。用户确认 W24-01 / W24-02，并追加主站进入电鹿页的默认配置与卡顿问题；本批仅 Web，不改小程序。

| 编号 | 优先级 / 状态 | 问题及证据 | 实施 / 验收 |
| --- | --- | --- | --- |
| W24-01 | P3 / 已完成本地验收 | “仅展示每条进化链的最终形态”与实际 `formRole=final` 口径不完全一致。 | 改为“仅展示已确认最终形态”；地＋武仍为 8 个结果。 |
| W24-02 | P3 / 已完成本地验收 | 双属性已在区域标题展示，卡片重复“武 · 地 · 二阶”。 | 卡片副信息只保留阶段；名称、头像、结果数不变。 |
| W24-03 | P1 / 已完成本地验收 | 主站选择电鹿后进入专页，未完成的当前配置会以“当前配置”进入，出现普通性格／空个体等无效默认；层数调整还会重复整表扫描。 | 当前电鹿配置不完整时自动套“常用配置”（开朗＋生命/物攻/速度 60）；完整当前配置继续原样带入。斩杀表扫描与“当前层数”选择拆开，普通层数增减不再重跑整套 0–99 层扫描。实测层数输入响应约 62.7ms，结果切换约 3.4ms。 |

方案对图：`artifacts/web-ux24/proposal/mockups-combined.png`。真实验收：`artifacts/web-ux24/acceptance/`。发布门禁：1917 项单测、13 项 E2E、Lint、生产构建及性能预算均通过。


## UX23 用户指定 · 性格阶梯选择 · 2026-09-20

方案修订已确认实施：六个增益分组名称字号 13→14px，去除分组前箭头；不改子项字号、行高、顶部选择框箭头或悬停／点击展开规则。
每行 +20% 左侧加入 18px 对应属性图标，复用既有六维图标、间距 8px；不是展开箭头或独立按钮。

基线 `2e4b1c4` 加本地快捷键改动，网页／Windows 桌面范围，小程序不改。1920×1080 具体版，音速犬胆小对水灵，亮暗同框截图。W23-01/02 已本地实施，未提交、未发布。计划见 `web-ux23-plan-20260920.md`，实际合图见 `artifacts/web-ux23/acceptance/preview.html`。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W23-01 | P2 / be642d7 已发布；默认展开追加已本地验收，未发布 | 用户确认阶梯界面，上线后追加“已有选择默认展开所属栏位”。 | 当前性格有增益时默认展开所属组，共 12 行；无修正 7 行。悬停／点击仍只展开一组 5 项，字号、图标、布局不变。30 个性格定位及重开、无修正已测；追加对照见 `artifacts/web-ux23/default-group-acceptance/preview.html`。 |
| W23-02 | P1 / 已本地实施，未发布 | 用户确认按住 Tab 编辑配置，松开返回且修改保留。 | 已拆分临时模式与连撤的取消逻辑；点击、聚焦、编辑不再提前退出，捕获松键与连发。验收覆盖真实原生性格选择、个体编辑、修改保留与撤回；阶梯菜单仍待确认。 |
| W23-03 | P2 / 已本地实施，未发布 | 用户确认“增益性格加颜色这个也做”。 | 仅性格增加的属性图标变绿，保留图形、尺寸、文字、数值及其他图标灰色；切换性格同步，无修正恢复全灰。主界面、队伍、能力分析与复用六维卡片一致；亮暗实际对照在 `artifacts/web-ux23/gain-acceptance/preview.html`。 |
| W23-QA | P2 / 待复核，未扩展本轮范围 | 扩展 app-integration 测试中“水灵设为攻击方”匹配两个按钮、“显示威力”位于折叠设置导致两项失败；使用 HEAD 原性格组件与原测试只读复核仍同样失败。 | 非新性格菜单回归；保留失败与基线复现证据，后续单独校正测试定位。发布测试集本轮通过。 |
| W23-04 | P2 / 已本地验收，未提交发布 | 用户指定在电鹿“下注·暗”下增加与通电相同的勾选入口。 | 勾选设自身HP为49%，取消100%；状态跟随实际整数生命是否低于半血，复用原伤害计算及勾选样式。75项专项、构建及亮暗/320/390实测通过；见 `web-ux23-acceptance-20260920.md` 追加记录。 |

## UX22 用户指定 · 布灵队伍系别统计 · 2026-09-20

基线 `0c6874e`，Web 2.2.2，1920×1080。用户于 2026-09-20 确认“布灵这个按照这个做”。按 ui-ux-preview 与 design-craft 沿用确认方案，只插入只读统计，不改侧栏或旧玩具层数。已本地实现并通过功能、视觉检查；后续按用户要求建立长期体积预算，完整构建已通过，未提交、未推送。阵容导出问题不在本轮实现范围，另有独立修复记录。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W22-01 | P2 / 已本地验收 | 用户“布灵专属功能；队伍里面，可以统计一下不同系别技能总数”。旧玩具已有“己方已使用不同技能系别数”输入，但队伍成员页无配招系别汇总。 | 仅在布灵／布灵布灵的成员配置页卡片下展示“旧玩具 · 队伍技能系别 N 种”及去重图标；统计当前队伍六个槽位的四技能，包括自身、防御和变化技能，同系只计一次。空槽和失效技能不计；与攻击技能打击面独立，不改该计算。 |

示例：加尔、徘徊爪爪、深蓝鲸、小皮球、布灵布灵、绒光优优，读取当前内置预设的 24 个技能槽，得到普通／武／萌／毒／火／水／幻／电／光／地共 10 种。它是配招可用系别，不是已使用次数；不自动写入旧玩具特性层数，不显示确定已获得的双攻加成。图片中的具体数值是上述示例，不认定用户截图配招与示例逐项相同。

证据：方案在 `artifacts/web-ux22/proposal/preview.html`；真实实现为 `actual-dark/light.png`、`actual-1920/390/320-dark/light.png`，同框合图 `acceptance/preview.html`。桌面条带宽 907px，窄屏 357/287px，scrollWidth 均等于 clientWidth；图标均加载，深浅色已读回。真实 UI 清空小皮球两项电系技能后，切回布灵统计从 10 降至 9；非布灵隐藏。测试会话存档已恢复，未改用户页面或存档。

验证：新增 3 项专项覆盖两形态、自身、非攻击技能、重复／空／失效项、6×4 边界、数据更新和只读；连同队伍面板共 33 项通过。实施时常规测试 104 文件 1806 项、lint、12 项 E2E 通过；当时完整构建被旧预算阻断。随后用户要求建立长期体积预算，见 `performance-budget-policy.md`；当前包含阵容导出修复的完整构建已通过。未发布状态不变。

## UX21 常用配置定向补池 · 2026-09-20

本轮确认与验收：用户“2改 13不改 然后提交看web”。仅 W21-02 已实施，W21-01／03 明确不改，后续不重复提案。别名、正式名、无结果、清空焦点与 230 项恢复通过；1920／390／320 亮暗无横向溢出。1806 项发布测试、23 项相关测试、12 项发布 E2E、lint、构建通过，JS 348.20／348.25 KiB、CSS 55.96／56 KiB。实际截图 `artifacts/web-ux21/acceptance/preview.html`；提交和线上回查见本轮验收记录，以下保留原方案证据。

基线 `53e4556` 加当前未提交改动，Web 2.2.2；旧候选已处理完，本轮只补查常用配置弹窗，不重复全站巡检。1920×1080 横屏、具体版、亮暗主题，独立浏览器的现有旧预设与当前 230 项预设对比。以下三项仅效果模拟，待用户确认；产品源码未改，未提交、未推送。详情 `web-ux21-plan-20260920.md`，合图 `artifacts/web-ux21/proposal/mockups-combined.png`。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W21-01 | 用户否决 / 不改 | 开关可见名称仍为“仅预览配置项”，而现有行为和 tooltip 已包括手动收藏。`w1-before-light.png`。 | 保留原可见名称，不实施拟议文案；此前已完成的手动收藏浏览及 tooltip 不回退。 |
| W21-02 | P2 / 已实施验收 | 配置清单搜索“圣甲虫”显示 0/230；搜索正式名“圣凯布米龙”显示 1/230。原源码仅匹配 fullName。`w2-before-light.png`。 | 已匹配已有 aliases，仍只查 sourceEntries；“圣甲虫”命中圣凯布米龙，不新增配置或修改配招。正式名、别名、空查询、无结果、清空后计数与焦点通过。 |
| W21-03 | 用户否决 / 不改 | 汇总“不同 10、新增 3”，按钮“更新配置（12）”；实际差异列表为 9 项将更新、1 项保留手改，数字计算正确。`w3-before-light.png`。 | 保留“相同／不同／新增”三项，未拆分统计，不改导入判定。 |
| W21-04 | 已排除 | 配置清单已有清除按钮、清空后聚焦搜索框，输入为空时禁用；源码和真实弹窗均有入口。 | 不重复增加清空按钮；与 W20-03 的承伤搜索不是同一控件。 |
| W21-05 | 已排除 | W21-03 实际 9 项将更新＋3 项新增＝12，1 项手改被保留。 | 不是漏导入或计数算法错误；只优化信息区分，不改保护手改配置的策略。 |

所有模拟逐项 try/finally 还原，返回 `restored: true`；亮暗截图和合成图已读回。当前只有方案证据，不代表真实功能或窄屏验收通过；待确认后按受影响范围实施测试。JS 预算仍为本次已批准的 348.25 KiB，不擅自上调。

## UX20 定向补池 · 2026-09-20

方案基线 `7a6720e`，实施基线 `53e4556`，Web 2.2.2。用户确认后，W20-01／02／03 均已完成本地实施验收；按后续“先别提交”要求，未提交、未推送。以下表格保留发现阶段证据及建议。验收见 `web-ux20-acceptance-20260920.md`、`artifacts/web-ux20/acceptance/preview.html`。主视口 1920×1080 横屏，亮暗及 320／390px 兼容检查通过；不是全站巡检。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W20-01 | P1 / 已完成，未提交 | 银月狼王对布灵布灵，目标最大生命 362；HP 输入 9999 并失焦仍显示 9999，伤害 263、剩余 99，说明计算按 362。百分比输入 120 后失焦仍为 120，上限 100。`hp-overflow.png`、`inspect.js`、`capture-proposals.js`。 | 失焦／回车时按现有计算口径同步规范值；输入途中允许编辑，空值沿用当前恢复规则。复核 HP／百分比、负数／超上限、满血按钮、切换目标及撤回；不改计算公式。 |
| W20-02 | P2 / 已完成，未提交 | 承伤对比改为全部形态、高到低，收起筛选后仍只显示“622 只 · 生命性格满双防个体”，看不见当前形态范围和排序。`w2-before-light.png`。 | 仅在筛选折叠时，汇总条右侧简述“全部形态 · 承伤高→低”；默认应明确最终形态＋首领、低到高。宽屏利用现有空位，不重复显示模板；窄屏确认后检验换行与空间。 |
| W20-03 | P3 / 已完成，未提交 | 承伤搜索有结果时没有清空入口，需要手动全选删除；空结果虽有“清除搜索与筛选”，却会同时重置承伤范围。`w3-before-light.png`，查询“银月”。 | 输入非空才显示清空叉号，只清 query，保留模板、范围、排序及状态选项；清空后焦点留在搜索框。空串不显示，补键盘名称与窄屏点击区域检查。 |
| W20-04 | 已排除 | 实测列表滚动 500px 后，表头 top 与滚动区 top 均为 435px，CSS 为 sticky。`comparison-scrolled-light.png`。 | 表头已吸顶，不重复提案。 |
| W20-05 | 已排除 | 关闭筛选后，从关闭按钮逐次 Tab：比较技能→搜索→筛选→范围按钮，无隐藏下拉被聚焦。`inspect-comparison.js`。 | 本路径正常；不是完整焦点循环或所有弹层已验收。 |
| W20-06 | 已排除 | 空结果已有“清除搜索与筛选”入口，源码 `DamageComparisonDialog.jsx`。 | 不新增第二个空状态重置按钮；W20-03 是有结果时仅清搜索的快捷操作，二者范围不同。 |

方案模拟已逐项还原，随后正式实现并验收。50 项相关组件测试、1793 项全量现有测试、12 项生产产物 E2E 及 lint 通过。用户本次批准 JS 硬上限 348.25 KiB，CSS 仍为 56 KiB，后续体积另议；实测 348.10／55.96 KiB，体积门禁通过，基线警告保留。当前候选已处理完，不重新扫描或新增改动，等待其他预设。

## UX19 定向补池 · 2026-09-20

基线 `ccdfcb8` 加本轮 W18-02 字号修复，Web 2.2.2。主视口 1920×1080、具体版、银月狼王对布灵布灵，亮暗主题。既有候选已完成，本轮仅补查主计算页结果区域和工具箱入口结构；不是全站巡检。两项均已按用户“处理”确认实施，本地验收完成，未发布。方案见 `web-ux19-plan-20260920.md`，实测见 `web-ux19-acceptance-20260920.md`、`artifacts/web-ux19/acceptance/preview.html`。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W19-01 | P2 / 已完成，未发布 | 选择非伤害技能“离魂术”，右栏红色“非伤害技能”容易混同异常提示；390px 底栏仍显示“待输入”，与已完整配置的状态不符。`artifacts/web-ux19/w1-before-light.png`、`status-before.png`。 | 已按确认实现主结果中性色、底栏“非伤害”；正常伤害及缺输入分支不变。亮暗、320/390px 实测及专项测试通过。 |
| W19-02 | P3 / 已完成，未发布 | 1920×1080 右侧四技能结果文字 11px、表头 10px，阅读偏小。`artifacts/web-ux19/w2-before-light.png`。 | 已统一正文 12px、表头 11px，行高仍 38px；宽屏列宽和边线不变。兼容检查发现 320px 抽屉固定最小列宽溢出，已仅在窄屏允许比例条收缩；320/390 两主题通过。长名称／99999／999.9% 合成边界检查通过，已还原测试文字。 |

已排除重复项：W15-05 已有 3px 选中内边线，实测亮色紫／暗色金均生效，修正下表旧状态，不再次提案。工具箱入口结构本轮未发现值得新增的改动，不据此宣称所有工具页已验收。两项临时模拟均已撤销；确认后正式实现及截图另存 acceptance，不覆盖原方案。当前候选池已处理完，下轮需要时再定向补池。

## UX18 定向问题 · 2026-09-20

基线 `6e52867`（Web 2.2.2 已发布）。仅核对用户新增截图，不扩展全站巡检。

追加 W18-02（P2，已完成，未发布）：用户指出模式切换时标题字号变化。基线 `ccdfcb8`、470×900 实测，精简标题 14px、具体标题 15px，字重均 800，并非截图缩放。用户“修复字的问题”后，仅窄屏统一为 14px，1920px 保持 18px；320/390/470/620 亮暗两种模式均实测通过。W18-01 仅统一按钮，漏验同一标题栏的标题；已在现有 E2E 加入标题字号／字重／行高比较，不另增冗余测试。验收见 `web-ux19-title-acceptance-20260920.md`。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W18-01 | P2 / 已完成，未发布 | 线上 470×900，窄屏单模式按钮：精简为 column / 透明底 / 外框 padding 1px，具体为 row / 金底 / padding 2px；旧精简样式与行内样式叠加，pressed 状态只为具体模式启用金底。 | 用户“完成 修复”确认后，已统一横排金底、42×38px 按钮及 48×44px 外框。320/390/470/620 亮暗连续切换、键盘切回与 621/1920/横屏双按钮边界通过。新增 1 项集中 E2E 防回归；详见 `web-ux18-header-acceptance-20260920.md`，实际图 `artifacts/web-ux18/acceptance/preview.html`。 |

## UX17 定向追加 · 2026-09-20

基线 `2b2ebd5`（Web 2.2.2 已发布）；仅针对用户截图，不重跑全站巡检。W17-01 / W17-02 均已按用户确认本地实施验收，尚未发布。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W17-04 | P2 / 已完成，未发布 | 2026-09-20 用户竖屏截图：完整成员卡占据约 38dvh，与编辑区形成上下两个滚动区。`06eb290` 取消 compact 入口同时影响了宽屏和窄屏，恢复范围过宽。 | 用户“改正确”后已修复。窄屏三列两行，320/390 成员区 183px，无内部滚动；当前成员操作按需展开。宽屏仍为 280px 完整侧栏，两页一致。回归检查已加入既有 E2E，见 `web-ux17-responsive-acceptance-20260920.md`。 |
| W17-03 | P2 / 已完成，未发布 | 基线 `1ef9825`，Web 1920×1080 亮色能力分析。手动微调自动列数导致 5＋1，面板宽 905px。 | 已按确认方向使用容器断点：至少 714px 六列，354–713px 三列，更窄保留两列；六组边界宽度验证无溢出。实际图见 `artifacts/web-ux17/responsive-acceptance/preview.html`，字体及性格区不变。 |
| W17-01 | P2 / 已完成，未发布 | 用户确认两行两列方案，追加上排使用五字“设为攻击方／设为防御方”；下排复制文案不变。 | 已复用 `onApply` / `onCaptureSide`，外向／内向图标和攻红防蓝区分方向；空位与待修复成员禁止设为，左侧入口保留。30 项队伍测试、四方向真实点击和 320/390/1440/1920 布局通过。见 `web-ux17-actions-acceptance-20260920.md`。 |
| W17-02 | P2 / 已完成，未发布 | 用户指出成员配置切到能力分析后侧栏缩窄、技能图标消失、攻防按钮搬到底部。`data-compact-roster` 在能力页启用，1200px 以上固定为 160px。 | 用户确认恢复图 3 后，取消能力页两个紧凑模式入口。两页侧栏在 1920/1440 下均为 280px，320/390 下卡片尺寸也一致；保留 6 组技能图标、12 个攻防按钮，不再渲染底部成员操作。见 `web-ux17-roster-acceptance-20260920.md`。 |

最后更新：2026-09-20。版本 2.2.2。UX15 / UX16 均已本地实施验收，未发布；现有候选及追加名称问题全部完成，下轮需要时再定向补池。最新记录见 [UX16 验收](web-ux16-acceptance-20260920.md)。

## 使用规则

主视口为 **1920×1080 横屏**（按用户“1080”理解为 1080p）；亮暗主题及精简/具体版按问题需要核对，320/390px 只做兼容检查。每轮先从本表挑最多 3 项，其他候选保留；池中还有候选时不重做全站扫描，耗尽或证据失效再补巡检。不为凑满 3 项制造改动。选中项实施前复现、实施后验收，记录状态与提交。

状态：待选 / 入选待确认 / 实施中 / 已完成 / 待复核 / 已排除。P1 为交互可靠性，P2 为主要可读性或操作反馈，P3 为次要视觉改善。重复问题复用原编号。

## 本轮发现（全部保留）

| 编号 | 优先级 / 状态 | 问题及证据 | 建议与验收要求 |
| --- | --- | --- | --- |
| W15-01 | P2 / 已完成 | 1920×1080、具体版、亮色，武斗酷猫对迪莫。双方特性控制区行数不同，技能表头顶边相差 **41px**；对应技能行难以横向比较。`artifacts/web-ux15/mock-w1-before.png`，源码 `FourSkillEditor.jsx` 的特性区与 `.skill-slot-list`。 | 仅双列模式对齐技能表头，按实际内容高度留白，不写死当前 41px；额外条件、字体变化后仍对齐，手机堆叠布局不留空白。不改变特性配置或计算。 |
| W15-02 | P2 / 已完成 | 1920×1080、具体版、亮色，天气选“雨天”再收起高级选项，标题仍只有“高级选项”。右侧虽有条件摘要，但折叠入口自身没有启用状态。`mock-w2-before.png`，`AdvancedOptions.jsx` 按钮只渲染标题与箭头。 | 折叠标题旁显示简短非默认条件，如“雨天”；多项只展示前两项与“+N”，全默认时不显示。只表示已配置，不冒充该条件对当前技能一定有增益；不重复展示主面板能力等级。 |
| W15-03 | P1 / 已完成 | 1920×1080、显示设置。没有顶部关闭入口；实测 Esc 后弹窗仍可见，从“完成”按 Tab 会跳到背后“撤回上一步（1）”。320px 时底部“完成”还需滚动才能找到。`mock-w3-before.png`、`settings-320-light.png`；组件 `DisplaySettingsDialog.jsx` 未实现键盘关闭/焦点限制。 | 顶部补关闭按钮，支持 Esc，Tab/Shift+Tab 保持在弹窗内，关闭后回到触发入口；保留底部“完成”，沿用即时保存，不添加保存确认。页面滚动锁与其他弹层兼容验证。 |
| W15-04 | P3 / 已完成 | 显示设置将静态和显示威力两段详细解释同时常驻，1920px 弹窗较长；320px 更占空间。与 W15-03 是不同问题，关闭可靠性先处理。证据同 `mock-w3-before.png`、`settings-320-light.png`。 | 可把完整口径说明折叠，默认只留当前模式简要说明；不删除公式口径，不增加页面。若用户认为说明需要常驻则排除。 |
| W15-05 | P3 / 已完成 | UX16 已实现 3px 主题色内侧边线；UX19 再次确认亮暗主题均生效。旧待确认状态已修正。 | 保留当前细边线、不增加文字、不改伤害风险颜色，不再重复提案。 |
| W15-06 | P2 / 已完成（窄屏兼容） | 桌面浏览器缩到 320px、精简四技能时，视口宽 320、可用 clientWidth 305、scrollWidth 320，存在 **15px** 水平超宽；右缘配置和菜单显得贴边。`four-320-light.png`。UX16 已定位 html/body 的 320px 最小宽度；取消后 scrollWidth 为 305px，但需同步保留标题和精灵名，详见 UX16 计划。 | 定向检查页面最小宽度与经典滚动条占宽；不得简单隐藏溢出掩盖内容。仅在兼容批次修复，不打断本轮横屏优先项；不等同真实手机已复现。 |
| W16-01 | P2 / 已完成 | 用户追加：窄屏选择框中的已选精灵名被单行输入裁切，320px 武斗酷猫显示不全。 | 未展开时完整展示、长名称换行，点击保留全选；Backspace 恢复默认候选，Esc 恢复。320/360/390及宽屏已验证，详见 UX16 验收。 |

## 上轮计划（UX15 已完成）

1. W15-01：双方技能列表对齐。
2. W15-02：折叠高级选项保留已配置条件摘要。
3. W15-03：显示设置关闭入口与键盘焦点完整性。

效果图：`artifacts/web-ux15/preview.html`、`mockups-combined.png`。图中 after 是临时 DOM/CSS 模拟，不代表已实施交互；每项截图后还原。截图产生的天气值已恢复为 none。用户已确认并实施；实测对照见 `artifacts/web-ux15/acceptance.html`，未推送发布。

## 当前批次（UX16 已完成）

从现有池选择 W15-04 / 05 / 06，不新增问题编号、不重跑全站巡检。基线 `986ba35`，已定向复核亮暗选中态、显示设置与经典滚动条超宽；全部临时注入已还原。

1. 设置完整口径说明折叠，保留当前模式一句话。
2. 选中结果行加 3px 主题色内侧边线。
3. 根节点按真实可用宽度布局；极窄屏同步压缩间距和头像，避免取消最小宽度后赛季名截断、四字精灵名换行。

方案：[UX16 计划](web-ux16-plan-20260920.md)；效果图 `artifacts/web-ux16/preview.html`、`mockups-combined.png`。用户已确认并追加选择框名称补全，全部实施；实测页 `artifacts/web-ux16/acceptance.html`，本批提交主题 `fix(web): 完成 UX16 优化并补全窄屏精灵名`。

## 已完成池（避免重复选取）

| 编号 | 项目 | 完成提交 / 证据 |
| --- | --- | --- |
| W13-01 | 窄屏普通精灵下拉展开 | `c0b3983`；`web-ux13-acceptance-20260920.md` |
| W13-02 | HP / % 当前单位高亮及满血同步 | `c0b3983`；同上 |
| W13-03 | 撤回默认位置随视口，保留拖动偏好 | `c0b3983`；同上；旧位置缓存继续保留是既定取舍 |
| W15-01 / 02 / 03 | 技能区对齐、折叠条件摘要、设置窗关闭与焦点 | `986ba35`；`web-ux15-acceptance-20260920.md` |
| W15-04 / 05 / 06、W16-01 | 设置说明收纳、选中行细边线、窄屏超宽及选择框全名 | 本批 `fix(web): 完成 UX16 优化并补全窄屏精灵名`；`web-ux16-acceptance-20260920.md` |
| W14-01 | 横排底栏与承伤对比统一 | `3be3171`；`web-ux14-acceptance-20260920.md`；单行横排为用户确认约束，不再推荐拆行 |

## 检查边界

本轮证据主要来自本地生产预览 4197、基线 3be3171；横屏主要为亮色具体版，本轮未做全站亮暗主题或全部弹层全覆盖。前期窄屏巡检发现一并记入，不因优先级调整丢弃。入选三项已补暗色及 320/390px 验收，详见 UX15 验收记录。小程序及桌面安装包本轮不在范围。

## UX23 定向候选 · 2026-09-22

| 编号 | 优先级 / 状态 | 问题与证据 | 计划 |
| --- | --- | --- | --- |
| UX23-01 | P3 / 已完成 | 属性查询两列标题右侧短句与标题下说明重复；用户截图 1。 | 已删除“别人打我”“我用这两系打谁”，保留标题下的具体口径。 |
| UX23-02 | P2 / 已完成 | 底部倍率来源说明条占用整行；用户截图 2。 | 双属性选择时展示已确认最终形态；单属性隐藏；倍率来源仅在点击结果后出现。 |

示意图：`artifacts/web-ux23/proposal/mockups-combined.png`。2026-09-22 已完成本地实现与 1280×760 / 390×844 验收。
