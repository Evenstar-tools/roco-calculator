# Web 小优化待办池

## UX19 定向补池 · 2026-09-20

基线 `ccdfcb8` 加本轮 W18-02 字号修复，Web 2.2.2。主视口 1920×1080、具体版、银月狼王对布灵布灵，亮暗主题。既有候选已完成，本轮仅补查主计算页结果区域和工具箱入口结构；不是全站巡检。两项均已按用户“处理”确认实施，本地验收完成，未发布。方案见 `web-ux19-plan-20260920.md`，实测见 `web-ux19-acceptance-20260920.md`、`artifacts/web-ux19/acceptance/preview.html`。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W19-01 | P2 / 已完成，未发布 | 选择非伤害技能“离魂术”，右栏红色“非伤害技能”容易混同异常提示；390px 底栏仍显示“待输入”，与已完整配置的状态不符。`artifacts/web-ux19/w1-before-light.png`、`status-before.png`。 | 已按确认实现主结果中性色、底栏“非伤害”；正常伤害及缺输入分支不变。亮暗、320/390px 实测及专项测试通过。 |
| W19-02 | P3 / 已完成，未发布 | 1920×1080 右侧四技能结果文字 11px、表头 10px，阅读偏小。`artifacts/web-ux19/w2-before-light.png`。 | 已统一正文 12px、表头 11px，行高仍 38px；宽屏列宽和边线不变。兼容检查发现 320px 抽屉固定最小列宽溢出，已仅在窄屏允许比例条收缩；320/390 两主题通过。长名称／99999／999.9% 合成边界检查通过，已还原测试文字。 |

已排除重复项：W15-05 已有 3px 选中内边线，实测亮色紫／暗色金均生效，修正下表旧状态，不再次提案。工具箱入口结构本轮未发现值得新增的改动，不据此宣称所有工具页已验收。两项临时模拟均已撤销；确认后正式实现及截图另存 acceptance，不覆盖原方案。当前候选池已处理完，下轮需要时再定向补池。

## UX18 定向问题 · 2026-09-20

基线 `6e52867`（Web 2.2.2 已发布）。仅核对用户新增截图，不扩展全站巡检。

追加 W18-02（P2，已完成，未发布）：用户指出模式切换时标题字号变化。基线 `ccdfcb8`、470×900 实测，精简标题 14px、具体标题 15px，字重均 800，并非截图缩放。用户“修复字的问题”后，仅窄屏统一为 14px，1920px 保持 18px；320/390/470/620 亮暗两种模式均实测通过。W18-01 仅统一按钮，漏验同一标题栏的标题；已在现有 E2E 加入标题字号／字重／行高比较，不另增冗余测试。验收见 `web-ux19-title-acceptance-20260920.md`。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W18-01 | P2 / 已完成，未发布 | 线上 470×900，窄屏单模式按钮：精简为 column / 透明底 / 外框 padding 1px，具体为 row / 金底 / padding 2px；旧精简样式与行内样式叠加，pressed 状态只为具体模式启用金底。 | 用户“完成 修复”确认后，已统一横排金底、42×38px 按钮及 48×44px 外框。320/390/470/620 亮暗连续切换、键盘切回与 621/1920/横屏双按钮边界通过。新增 1 项集中 E2E 防回归；详见 `web-ux18-header-acceptance-20260920.md`，实际图 `artifacts/web-ux18/acceptance/preview.html`。 |

## UX17 定向追加 · 2026-09-20

基线 `2b2ebd5`（Web 2.2.2 已发布）；仅针对用户截图，不重跑全站巡检。W17-01 / W17-02 均已按用户确认本地实施验收，尚未发布。

| 编号 | 优先级 / 状态 | 证据与问题 | 建议 / 验收 |
| --- | --- | --- | --- |
| W17-04 | P2 / 已完成，未发布 | 2026-09-20 用户竖屏截图：完整成员卡占据约 38dvh，与编辑区形成上下两个滚动区。`06eb290` 取消 compact 入口同时影响了宽屏和窄屏，恢复范围过宽。 | 用户“改正确”后已修复。窄屏三列两行，320/390 成员区 183px，无内部滚动；当前成员操作按需展开。宽屏仍为 280px 完整侧栏，两页一致。回归检查已加入既有 E2E，见 `web-ux17-responsive-acceptance-20260920.md`。 |
| W17-03 | P2 / 已完成，未发布 | 基线 `1ef9825`，Web 1920×1080 亮色能力分析。手动微调自动列数导致 5＋1，面板宽 905px。 | 已按确认方向使用容器断点：至少 714px 六列，354–713px 三列，更窄保留两列；六组边界宽度验证无溢出。实际图见 `artifacts/web-ux17/responsive-acceptance/preview.html`，字体及性格区不变。 |
| W17-01 | P2 / 已完成，未发布 | 用户确认两行两列方案，追加上排使用五字“设为攻击方／设为防御方”；下排复制文案不变。 | 已复用 `onApply` / `onCaptureSide`，外向／内向图标和攻红防蓝区分方向；空位与待修复成员禁止设为，左侧入口保留。30 项队伍测试、四方向真实点击和 320/390/1440/1920 布局通过。见 `web-ux17-actions-acceptance-20260920.md`。 |
| W17-02 | P2 / 已完成，未发布 | 用户指出成员配置切到能力分析后侧栏缩窄、技能图标消失、攻防按钮搬到底部。`data-compact-roster` 在能力页启用，1200px 以上固定为 160px。 | 用户确认恢复图 3 后，取消能力页两个紧凑模式入口。两页侧栏在 1920/1440 下均为 280px，320/390 下卡片尺寸也一致；保留 6 组技能图标、12 个攻防按钮，不再渲染底部成员操作。见 `web-ux17-roster-acceptance-20260920.md`。 |

最后更新：2026-09-20。版本 2.2.2。UX15 / UX16 均已本地实施验收，未发布；现有候选及追加名称问题全部完成，下轮需要时再定向补池。最新记录见 [UX16 验收](web-ux16-acceptance-20260920.md)。

## 使用规则

主视口为 **1920×1080 横屏**（按用户“1080”理解为 1080p）；亮暗主题及精简/具体版按问题需要核对，320/390px 只做兼容检查。每轮先从本表挑最多 3 项，其他候选保留；池中还有候选时不重做全站扫描，耗尽或证据失效再补巡检。不为凑满 3 项制造改动。选中项实施前复现、实施后验收，记录状态与提交。

状态：待选 / 入选待确认 / 实施中 / 已完成 / 待复核 / 已排除。P1 为交互可靠性，P2 为主要可读性或操作反馈，P3 为次要视觉改善。重复问题复用原编号。

## 本轮发现（全部保留）

| 编号 | 优先级 / 状态 | 问题及证据 | 建议与验收要求 |
| --- | --- | --- | --- |
| W15-01 | P2 / 已完成 | 1920×1080、具体版、亮色，武斗酷猫对迪莫。双方特性控制区行数不同，技能表头顶边相差 **41px**；对应技能行难以横向比较。`artifacts/web-ux15/mock-w1-before.png`，源码 `FourSkillEditor.jsx` 的特性区与 `.skill-slot-list`。 | 仅双列模式对齐技能表头，按实际内容高度留白，不写死当前 41px；额外条件、字体变化后仍对齐，手机堆叠布局不留空白。不改变特性配置或计算。 |
| W15-02 | P2 / 已完成 | 1920×1080、具体版、亮色，天气选“雨天”再收起高级选项，标题仍只有“高级选项”。右侧虽有条件摘要，但折叠入口自身没有启用状态。`mock-w2-before.png`，`AdvancedOptions.jsx` 按钮只渲染标题与箭头。 | 折叠标题旁显示简短非默认条件，如“雨天”；多项只展示前两项与“+N”，全默认时不显示。只表示已配置，不冒充该条件对当前技能一定有增益；不重复展示主面板能力等级。 |
| W15-03 | P1 / 已完成 | 1920×1080、显示设置。没有顶部关闭入口；实测 Esc 后弹窗仍可见，从“完成”按 Tab 会跳到背后“撤回上一步（1）”。320px 时底部“完成”还需滚动才能找到。`mock-w3-before.png`、`settings-320-light.png`；组件 `DisplaySettingsDialog.jsx` 未实现键盘关闭/焦点限制。 | 顶部补关闭按钮，支持 Esc，Tab/Shift+Tab 保持在弹窗内，关闭后回到触发入口；保留底部“完成”，沿用即时保存，不添加保存确认。页面滚动锁与其他弹层兼容验证。 |
| W15-04 | P3 / 已完成 | 显示设置将静态和显示威力两段详细解释同时常驻，1920px 弹窗较长；320px 更占空间。与 W15-03 是不同问题，关闭可靠性先处理。证据同 `mock-w3-before.png`、`settings-320-light.png`。 | 可把完整口径说明折叠，默认只留当前模式简要说明；不删除公式口径，不增加页面。若用户认为说明需要常驻则排除。 |
| W15-05 | P3 / 已完成 | UX16 已实现 3px 主题色内侧边线；UX19 再次确认亮暗主题均生效。旧待确认状态已修正。 | 保留当前细边线、不增加文字、不改伤害风险颜色，不再重复提案。 |
| W15-06 | P2 / 已完成（窄屏兼容） | 桌面浏览器缩到 320px、精简四技能时，视口宽 320、可用 clientWidth 305、scrollWidth 320，存在 **15px** 水平超宽；右缘配置和菜单显得贴边。`four-320-light.png`。UX16 已定位 html/body 的 320px 最小宽度；取消后 scrollWidth 为 305px，但需同步保留标题和精灵名，详见 UX16 计划。 | 定向检查页面最小宽度与经典滚动条占宽；不得简单隐藏溢出掩盖内容。仅在兼容批次修复，不打断本轮横屏优先项；不等同真实手机已复现。 |
| W16-01 | P2 / 已完成 | 用户追加：窄屏选择框中的已选精灵名被单行输入裁切，320px 武斗酷猫显示不全。 | 未展开时完整展示、长名称换行，点击保留全选；Backspace 恢复默认候选，Esc 恢复。320/360/390及宽屏已验证，详见 UX16 验收。 |

## 上轮计划（UX15 已完成）

1. W15-01：双方技能列表对齐。
2. W15-02：折叠高级选项保留已配置条件摘要。
3. W15-03：显示设置关闭入口与键盘焦点完整性。

效果图：`artifacts/web-ux15/preview.html`、`mockups-combined.png`。图中 after 是临时 DOM/CSS 模拟，不代表已实施交互；每项截图后还原。截图产生的天气值已恢复为 none。用户已确认并实施；实测对照见 `artifacts/web-ux15/acceptance.html`，未推送发布。

## 当前批次（UX16 已完成）

从现有池选择 W15-04 / 05 / 06，不新增问题编号、不重跑全站巡检。基线 `986ba35`，已定向复核亮暗选中态、显示设置与经典滚动条超宽；全部临时注入已还原。

1. 设置完整口径说明折叠，保留当前模式一句话。
2. 选中结果行加 3px 主题色内侧边线。
3. 根节点按真实可用宽度布局；极窄屏同步压缩间距和头像，避免取消最小宽度后赛季名截断、四字精灵名换行。

方案：[UX16 计划](web-ux16-plan-20260920.md)；效果图 `artifacts/web-ux16/preview.html`、`mockups-combined.png`。用户已确认并追加选择框名称补全，全部实施；实测页 `artifacts/web-ux16/acceptance.html`，本批提交主题 `fix(web): 完成 UX16 优化并补全窄屏精灵名`。

## 已完成池（避免重复选取）

| 编号 | 项目 | 完成提交 / 证据 |
| --- | --- | --- |
| W13-01 | 窄屏普通精灵下拉展开 | `c0b3983`；`web-ux13-acceptance-20260920.md` |
| W13-02 | HP / % 当前单位高亮及满血同步 | `c0b3983`；同上 |
| W13-03 | 撤回默认位置随视口，保留拖动偏好 | `c0b3983`；同上；旧位置缓存继续保留是既定取舍 |
| W15-01 / 02 / 03 | 技能区对齐、折叠条件摘要、设置窗关闭与焦点 | `986ba35`；`web-ux15-acceptance-20260920.md` |
| W15-04 / 05 / 06、W16-01 | 设置说明收纳、选中行细边线、窄屏超宽及选择框全名 | 本批 `fix(web): 完成 UX16 优化并补全窄屏精灵名`；`web-ux16-acceptance-20260920.md` |
| W14-01 | 横排底栏与承伤对比统一 | `3be3171`；`web-ux14-acceptance-20260920.md`；单行横排为用户确认约束，不再推荐拆行 |

## 检查边界

本轮证据主要来自本地生产预览 4197、基线 3be3171；横屏主要为亮色具体版，本轮未做全站亮暗主题或全部弹层全覆盖。前期窄屏巡检发现一并记入，不因优先级调整丢弃。入选三项已补暗色及 320/390px 验收，详见 UX15 验收记录。小程序及桌面安装包本轮不在范围。
