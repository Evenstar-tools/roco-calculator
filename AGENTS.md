# 洛克计算器项目协作规则

## 飞书安装包交付

以下规则在用户提出“发我飞书”“发送最新版安装包”“把桌面包发给我”等请求时强制执行。

1. **只走飞书 CLI，不用界面自动化。** 禁止为安装包交付调用 `computer-use`、浏览器、网页搜索或飞书 UI。全程使用 `lark-cli` 的用户身份。
2. **固定收件会话。** 收件人是“晚星”，P2P 会话固定为 `oc_2154a409474e928fdc827f2aaf2673ad`。直接向该 `chat_id` 发送；不得重新搜索联系人，不得发给“小虾”。只有该会话失效时才重新解析联系人。
3. **只做必要的本地核对。** 从 `package.json` 读取版本号，安装包必须位于 `installers/v<版本号>/洛克计算器-<版本号>.exe`。重新计算 SHA-256，并与同目录 `SHA256SUMS.txt` 核对。已完成测试和打包且文件未变化时，不重复跑测试、构建或打包。
4. **上传只传安装包。** 在项目根目录执行：

   ```powershell
   lark-cli drive +upload --as user --file "./installers/v<版本号>/洛克计算器-<版本号>.exe" --name "洛克计算器-<版本号>.exe"
   ```

   不上传单独的 SHA 文本，不搜索刚上传的文件；以 `drive +upload` 返回的 `ok: true`、文件名、大小和 URL 作为上传结果。不得假设同名覆盖会复用旧 URL，消息必须使用本次上传返回的 URL。若本地已有相同 SHA-256 的有效飞书链接，可直接复用，不重复上传。
5. **上传成功立即发链接。** 消息只保留版本号、安装包 URL、SHA-256 和一句验证状态；发送前按全局规则完成 UTF-8 输出门禁，然后立即执行：

   ```powershell
   lark-cli im +messages-send --as user --chat-id "oc_2154a409474e928fdc827f2aaf2673ad" --markdown <消息正文>
   ```

   消息模板、本地版本号和 SHA-256 必须在上传前准备好。`上传 EXE → 从返回值取得 URL → 补入消息 → 输出门禁 → 发送 → 回读` 必须放在**同一个 `exec_command` 或同一个本地脚本**中连续执行；上传成功后不得返回模型思考，也不得插入 Git 检查、云盘搜索、联系人搜索、状态汇报或其他工具调用。

6. **只做一次消息回读。** 发送后执行一次 `im +chat-messages-list --chat-id ... --order desc --page-size 1`，确认最新消息的版本号、URL、SHA-256 和中文无乱码；通过即结束，不再做云盘搜索或额外身份检查。
7. **失败时才诊断。** 默认复用现有授权。只有上传、发送或回读失败时，才运行 `lark-cli doctor`、`lark-cli auth status`、联系人解析或权限诊断。
8. **记录耗时。** 记录“开始上传、上传完成、消息发出、回读完成”四个本地时间；正常交付目标为上传完成后 15 秒内发出消息。最终汇报总耗时和上传后等待时间；超过 15 秒必须明确说明卡在哪一步。

最短有效路径固定为：`本地哈希核对并预制消息 → 单次编排完成云盘上传 EXE、固定会话发链接和回读`。

## 发布口径

- “已测试”不等于“已打包”，“已上传云盘”不等于“已发送给晚星”，三者必须分开记录。
- 工作树有未提交改动时，安装包必须由当前工作树重新构建；不得发送同版本目录中的旧包。
- 每次打包前先补齐 `CHANGELOG.md` 和 `src/data/user-release-notes.js`，再执行项目发布门禁。
